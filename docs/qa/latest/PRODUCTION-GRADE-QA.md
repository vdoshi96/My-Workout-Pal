# Production-grade audit QA

September 23, 2026 (America/Chicago). Implementation and browser-test reference: `6e72016cfeafd0e187266a747161c2f903eaebec`. Branch: `vishal/production-grade-audit`. The following closeout changes documentation and screenshot evidence only. All gates are repeated serially on that final commit before publication; the exact final timing lines and commit are included in the PR and owner handoff. This report retains pass counts and exit codes, with variable elapsed times omitted.

**W5 remains blocked by preserved assertions and a recovery-contract conflict. W3 also failed during one development-server refresh. The plan's completion bar is not met.** The owner authorized the bounded stale-journey edits described below and requested PR #8 be marked ready for review. That review status does not authorize merging or deployment. The four acceptance files remain byte-identical to `9d5162d`.

## 1. Win conditions W1–W7

| Condition | Result | Command | Output |
| --- | --- | --- | --- |
| W1 | Pass; exit 0 | `npx vitest run tests/unit/production-audit-copy.test.ts tests/unit/production-audit-contracts.test.ts` | `Test Files  2 passed (2)`<br>`Tests  96 passed (96)` |
| W2 | Pass; exit 0 | `pnpm test:e2e:authenticated -- production-audit` | `14 passed` |
| W3 | Inconsistent development navigation; final committed-run output is in the PR and owner handoff | `pnpm exec playwright test production-audit-public --project chromium-phone --project chromium-desktop` | Preflight: `12 passed`<br>First closeout attempt: `1 failed`, `11 passed (20.6s)` |
| W4 | Pass; exit 0 | `pnpm verify` | `Test Files  129 passed (129)`<br>`Tests  965 passed (965)`<br>`Everything's fine 🐶🔥`<br>`Test Files  4 passed (4)`<br>`Tests  34 passed (34)`<br>`seed:check passed: 27 required variation(s) have exactly two approved videos.`<br>`Verified generated service worker.`<br>`Verified 71 documentation files.`<br>`Production route boundary verified (47 App Router entries).` |
| W5, authenticated | Fail; exit 1; all 88 cases completed | `pnpm test:e2e:authenticated` | `21 failed`<br>`13 skipped`<br>`54 passed` |
| W5, public release | Fail; exit 1; all 168 cases completed | `pnpm test:e2e:release` | `9 failed`<br>`66 skipped`<br>`93 passed` |
| W6 | Pass; exit 0 | `pnpm docs:build`, `pnpm docs:check` | `Rendered 71 documentation files.`<br>`Verified 71 documentation files.`<br>32 required screenshots at the prescribed dimensions |
| W7 | Delivered | Section 10 report | This file and the owner handoff include all five required report sections. |

Both full W5 suites ran to completion on unchanged detached main `c619ac61316f91a1a096867f93df5b649282663e` in the canonical checkout. The branch was restored before edits. Both branch suites also ran to completion. Focused diagnostic subsets are not represented as full-suite results. No test files, blocks, snapshots, skips, retries, or longer timeouts were added. Every browser assertion remains; the only removed unit assertion was the duplicate specifically identified by the owner. Geometry and overflow thresholds remain unchanged.

W3 uses a task-owned dev server on port 3118 to avoid another project's existing port 3000 server. W4 includes typecheck, lint, the full unit/integration suite, database checks, seed policy, service-worker parity, documentation parity, the production build, and the route boundary. These are local checks; no migration or seed command ran. Generated Next.js caches were moved aside between public development and production checks; configuration and dependencies remain unchanged.

### Main-versus-branch W5 results

| Suite | Main `c619ac6` | Branch | Exit, main / branch |
| --- | --- | --- | --- |
| Authenticated | `42 failed`<br>`13 skipped`<br>`19 passed` | `21 failed`<br>`13 skipped`<br>`54 passed` | `1 / 1` |
| Public release | `36 failed`<br>`42 skipped`<br>`54 passed` | `9 failed`<br>`66 skipped`<br>`93 passed` | `1 / 1` |

The audit adds 14 authenticated acceptance cases and 12 running public acceptance cases; 24 public acceptance project instances are excluded by existing conditions. Main's 13 authenticated and 42 public skips therefore correspond to the branch's 13 and 66 skips. These are existing exclusions, not new skips. No main-passing test fails on the branch after the runner fixes. The tables below record every failing main title and every affected project, and identify the failures that remain.

### Authenticated: every title that failed on main

| File and test title | Failed on main `c619ac6` | Fails on the branch |
| --- | --- | --- |
| `tests/authenticated-e2e/animal-surface-pilot-authenticated.spec.ts` — verified, unverified, empty, and active member states keep the fox decorative | `chromium-desktop`, `webkit-phone` | `chromium-desktop`, `webkit-phone` |
| `tests/authenticated-e2e/animal-surface-pilot-authenticated.spec.ts` — member decoration is static, forced-color safe, and failure safe | `chromium-desktop`, `webkit-phone` | `chromium-desktop`, `webkit-phone` |
| `tests/authenticated-e2e/animal-surface-pilot-authenticated.spec.ts` — slow and failed personal-home reads stay truthful and recover through retry | `chromium-desktop` | `chromium-desktop` |
| `tests/authenticated-e2e/animal-surface-rollout-authenticated.spec.ts` — member rollout surfaces preserve product priority across the authenticated matrix | `chromium-desktop`, `webkit-phone`, `chromium-phone`, `chromium-tablet`, `webkit-tablet`, `webkit-desktop` | `chromium-desktop`, `webkit-phone`, `chromium-phone`, `chromium-tablet`, `webkit-tablet`, `webkit-desktop` |
| `tests/authenticated-e2e/animal-surface-rollout-authenticated.spec.ts` — owned companion failure collapses without changing protected controls | `chromium-desktop`, `webkit-phone` | None |
| `tests/authenticated-e2e/customization-geometry.spec.ts` — customization surfaces preserve geometry and media preferences | `chromium-desktop`, `webkit-phone`, `chromium-phone`, `chromium-tablet`, `webkit-tablet`, `webkit-desktop` | `webkit-phone`, `chromium-phone` |
| `tests/authenticated-e2e/firebase-auth-hydration.spec.ts` — full-page Settings fails closed until the browser Firebase identity is restored | `chromium-desktop`, `webkit-phone` | None |
| `tests/authenticated-e2e/flexible-routine-publication.spec.ts` — a custom flexible routine survives publication, workout snapshots, and equipment revision | `chromium-desktop`, `webkit-phone` | `chromium-desktop`, `webkit-phone` |
| `tests/authenticated-e2e/library-core-conditioning-expansion.spec.ts` — a verified member publishes, reloads, and starts all owned logging shapes | `chromium-desktop`, `webkit-phone` | `chromium-desktop`, `webkit-phone` |
| `tests/authenticated-e2e/library-guidance.spec.ts` — browses, creates, links, selects, and isolates private movements | `chromium-desktop`, `webkit-phone` | None |
| `tests/authenticated-e2e/library-strength-expansion.spec.ts` — publishes, reloads, and starts a routine with text-only upper- and lower-body additions | `chromium-desktop`, `webkit-phone` | None |
| `tests/authenticated-e2e/onboarding.spec.ts` — new accounts choose one idempotent example or blank graph through onboarding | `chromium-desktop`, `webkit-phone` | None |
| `tests/authenticated-e2e/onboarding.spec.ts` — both synthetic owners onboard while unverified and foreign states fail closed | `chromium-desktop`, `webkit-phone` | `chromium-desktop`, `webkit-phone` |
| `tests/authenticated-e2e/onboarding.spec.ts` — a failed onboarding retry keeps the same idempotency key and never claims success early | `chromium-desktop`, `webkit-phone` | None |
| `tests/authenticated-e2e/onboarding.spec.ts` — imperial editor typing stays literal and browser Back protects a dirty draft | `chromium-desktop`, `webkit-phone` | None |
| `tests/authenticated-e2e/onboarding.spec.ts` — owned customization publishes once, preserves history, and derives private insights | `chromium-desktop`, `webkit-phone` | `chromium-desktop`, `webkit-phone` |
| `tests/authenticated-e2e/onboarding.spec.ts` — an incompatible private movement blocks an editor equipment change without writing a revision | `chromium-desktop` | None |
| `tests/authenticated-e2e/onboarding.spec.ts` — an accepted runner save reconciles after an error response, replays once, and reaches immutable history | `chromium-desktop`, `webkit-phone` | None |

### Public release: every title that failed on main

| File and test title | Failed on main `c619ac6` | Fails on the branch |
| --- | --- | --- |
| `tests/e2e/animal-surface-pilot.spec.ts` — landing first viewport reproduces the selected board hierarchy | `chromium-phone`, `chromium-tablet`, `chromium-desktop` | `chromium-phone`, `chromium-tablet`, `chromium-desktop` |
| `tests/e2e/animal-surface-rollout.spec.ts` — public Library companion is silent and non-overlapping across the rollout matrix | `chromium-phone`, `webkit-phone`, `chromium-tablet`, `chromium-desktop`, `webkit-tablet`, `webkit-desktop` | None |
| `tests/e2e/animal-surface-rollout.spec.ts` — public Library keeps keyboard and screen-reader meaning independent from art | `chromium-phone`, `webkit-phone`, `chromium-tablet`, `chromium-desktop`, `webkit-tablet`, `webkit-desktop` | None |
| `tests/e2e/public-release.spec.ts` — guest previews both profiles and completes the public discovery route | `chromium-phone`, `webkit-phone`, `chromium-tablet`, `chromium-desktop`, `webkit-tablet`, `webkit-desktop` | None |
| `tests/e2e/public-release.spec.ts` — keyboard, phone targets, dark mode, and reduced motion preserve the public route | `chromium-phone`, `webkit-phone`, `chromium-tablet`, `chromium-desktop`, `webkit-tablet`, `webkit-desktop` | None |
| `tests/e2e/animal-surface-pilot.spec.ts` — public pilot surfaces stay decorative, bounded, and truthful across required widths | `webkit-phone`, `chromium-desktop` | `webkit-phone`, `chromium-desktop` |
| `tests/e2e/animal-surface-pilot.spec.ts` — dark and reduced-motion rendering keep both public companions intact | `webkit-phone`, `chromium-desktop` | `webkit-phone`, `chromium-desktop` |
| `tests/e2e/animal-surface-pilot.spec.ts` — forced colors, image failure, and 200 percent zoom collapse decoration safely | `chromium-desktop` | `chromium-desktop` |
| `tests/e2e/animal-surface-pilot.spec.ts` — public companion stays outside keyboard focus and the accessibility name graph | `chromium-desktop` | `chromium-desktop` |
| `tests/e2e/animal-surface-rollout.spec.ts` — public Library decoration is dark, reduced-motion, forced-color, and failure safe | `chromium-desktop` | None |
| `tests/e2e/animal-surface-rollout.spec.ts` — CDP page scale simulation is recorded separately from native 200 percent zoom | `chromium-desktop` | None |
| `tests/e2e/pwa-offline.spec.ts` — restores every opened public companion route offline without caching owned surfaces | `chromium-desktop` | None |

### Test-driven evidence

The original unchanged unit acceptance run failed with `Test Files  2 failed (2)` and `Tests  86 failed | 10 passed (96)` before implementation. The rest-timer assertion expected its existing timer after `next_set` and received `undefined`; prefill, discard, and pending-save checks also failed. Phase A through G each ran the unit acceptance command, which now passes all 96 checks.

For the singular-count follow-up, the existing member-home file first reported `1 failed | 3 passed (4)` when its row assertion required `<small>1 movement</small>`. The source and fixture changes then produced `4 passed (4)`. The duplicate plural assertion became one singular assertion, and the bare Mobility text check became a row-specific destination check.

Main-passing runner and Quiet Set journeys exposed application regressions after their required control updates. Finish accepted a click before the preceding exercise save completed; it now waits for pending operations and the local device write. A suspended offline tab could overwrite the online tab's connectivity through the shared durable merge; adopting a committed write now preserves the calling tab's connectivity while merging workout data and deriving save status. The existing unit harness and runner tests pass `44 passed (44)`. All seven desktop resilience cases then passed, including explicit retry without an online event and confirmed completion across suspended tabs. A subsequent complete run reproduced stale-tab navigation: selecting a set inherited an older timestamp and could lose to a stale shared projection. The UI now timestamps actual set and exercise changes. WebKit also exposed Settings hydration mismatches because its 445 supported time-zone names differ from Node's 418. Settings now receives one server-generated choice list through both the real page and fixture; no hydration warnings are suppressed. The existing focused unit files pass `171 passed (171)`. No new test blocks were added.

The first committed closeout run completed with `23 failed`, `13 skipped`, and `52 passed (8.2m)` in the authenticated suite. It exposed two additional failures: an accessibility scan measured 2.84:1 contrast on Finish while its disabled state changed, and the main-passing WebKit divergent-tab journey retained only one operation. Disabled runner controls now keep full contrast and use a dashed border. The timestamp fix is limited to navigation because timestamping unsaved drafts let one tab replace another tab's value before both saves. The existing focused checks then passed `Test Files  5 passed (5)`, `Tests  170 passed (170)`, and `8 passed (35.7s)` across desktop and WebKit phone. The eight browser cases cover interrupted-save accessibility, distinct offline saves, divergent values, and suspended-tab completion. Their assertions remain unchanged.

### Screenshot evidence and limits

All 32 required PNGs are under `docs/qa/latest/production-grade/`, at 390×844 and 1440×1000. The phone and desktop member captures were retaken, including Today and the routine editor with a one-movement routine. Public captures use local Chromium; member captures use synthetic fixture data in WebKit on phone and Chromium on desktop. The changed Today, routine collection, editor error, set-entry, and end-dialog views were visually inspected at both widths. Additional Settings, Library, and landing views were checked. All 32 capture measurements record no horizontal overflow.

Synthetic scopes are deleted after capture. YouTube requests use inert local HTML; the images do not prove playback, human video review, hosted authentication, production durability, native-device behavior, email delivery, or real account deletion. No production data, Firebase identity, Neon resource, Vercel setting, schema, seed, migration, dependency, or artwork changed. Superseded generated evidence was removed from `docs/qa/latest/` after replacement verification; intentional history remains in Git.

| View | Phone | Desktop |
| --- | --- | --- |
| `landing` | [landing-phone.png](production-grade/landing-phone.png) | [landing-desktop.png](production-grade/landing-desktop.png) |
| `program` | [program-phone.png](production-grade/program-phone.png) | [program-desktop.png](production-grade/program-desktop.png) |
| `program-push` | [program-push-phone.png](production-grade/program-push-phone.png) | [program-push-desktop.png](production-grade/program-push-desktop.png) |
| `library` | [library-phone.png](production-grade/library-phone.png) | [library-desktop.png](production-grade/library-desktop.png) |
| `library-push-up` | [library-push-up-phone.png](production-grade/library-push-up-phone.png) | [library-push-up-desktop.png](production-grade/library-push-up-desktop.png) |
| `progress` | [progress-phone.png](production-grade/progress-phone.png) | [progress-desktop.png](production-grade/progress-desktop.png) |
| `sign-in` | [sign-in-phone.png](production-grade/sign-in-phone.png) | [sign-in-desktop.png](production-grade/sign-in-desktop.png) |
| `not-found` | [not-found-phone.png](production-grade/not-found-phone.png) | [not-found-desktop.png](production-grade/not-found-desktop.png) |
| `member-today` | [member-today-phone.png](production-grade/member-today-phone.png) | [member-today-desktop.png](production-grade/member-today-desktop.png) |
| `member-onboarding-step2` | [member-onboarding-step2-phone.png](production-grade/member-onboarding-step2-phone.png) | [member-onboarding-step2-desktop.png](production-grade/member-onboarding-step2-desktop.png) |
| `member-settings-presetup` | [member-settings-presetup-phone.png](production-grade/member-settings-presetup-phone.png) | [member-settings-presetup-desktop.png](production-grade/member-settings-presetup-desktop.png) |
| `member-routine-editor-errors` | [member-routine-editor-errors-phone.png](production-grade/member-routine-editor-errors-phone.png) | [member-routine-editor-errors-desktop.png](production-grade/member-routine-editor-errors-desktop.png) |
| `member-routines` | [member-routines-phone.png](production-grade/member-routines-phone.png) | [member-routines-desktop.png](production-grade/member-routines-desktop.png) |
| `member-library-guide` | [member-library-guide-phone.png](production-grade/member-library-guide-phone.png) | [member-library-guide-desktop.png](production-grade/member-library-guide-desktop.png) |
| `runner-set-entry` | [runner-set-entry-phone.png](production-grade/runner-set-entry-phone.png) | [runner-set-entry-desktop.png](production-grade/runner-set-entry-desktop.png) |
| `runner-end-dialog` | [runner-end-dialog-phone.png](production-grade/runner-end-dialog-phone.png) | [runner-end-dialog-desktop.png](production-grade/runner-end-dialog-desktop.png) |

## 2. Items from sections 3–7 not implemented

No prescribed implementation item was omitted. The section 6 reset-only code is present, but the successful server-error recovery behavior remains unverified because of the documented contract conflict below. W5 is incomplete, so this is not a completed-plan or release claim.

### Stopped items under the owner's boundary

- **Retired landing composition:** `tests/e2e/animal-surface-pilot.spec.ts` still checks the old single-node/four-word heading geometry and `[data-companion-placement="landing"]` side illustration. The current heading contains a line break, and `.quiet-studio` is a background composition. It has no equivalent separate rectangle for the old non-intersection and grid-collapse assertions. Those assertions remain unchanged. Positive image-loading checks in public release/PWA use the current image; the old geometry checks are not replaced by weaker image-presence checks.
- **Removed empty Today progress:** `tests/authenticated-e2e/animal-surface-pilot-authenticated.spec.ts` and `onboarding.spec.ts` still require `No completed workouts yet`. The current Today page has no empty progress card. The pilot also retains checks for removed `.member-program-actions`, `.member-home-progress` in the empty state, and `.member-equipment` on Today. The old co-located controls have no single equivalent on that screen.
- **Removed Today cardio summary:** `flexible-routine-publication.spec.ts` still requires `1 movement · no cardio` on the day row. Today now shows only the movement count. The count is singular in the app and test; the missing cardio summary is not dropped from the assertion.
- **Integrated member artwork:** the authenticated pilot and rollout retain the original non-intersection limit of 1 square pixel. The current Today and member Library scenes overlap the old protected rectangles. A separate decorative-card layout has no equivalent in this composition. Artwork and the geometric thresholds remain unchanged.
- **Hidden phone identity:** `customization-geometry.spec.ts` requires the account identity, display name, and verification label in the header. The existing phone stylesheet hides that block at widths up to 700 px. There is no equivalent visible identity block in the phone header; those assertions remain unchanged.
- **Hidden editor metadata:** `library-core-conditioning-expansion.spec.ts` retains its four visible section/logging-type assertions, with the current human-readable labels. The existing stylesheet hides those metadata spans. Current inputs already have separate assertions; substituting those same input checks would remove the metadata-visibility coverage.
- **Removed Settings explanation:** `onboarding.spec.ts` retains the assertion for `Stored workout measurements remain in canonical…`. The plan removes that implementation-language explanation and the current Settings page has no equivalent sentence about storage units. The test reaches this point after publication recovery, immutable history, and equipment revision checks.
- **Recovery contract conflict:** `animal-surface-pilot-authenticated.spec.ts` retains its successful-retry assertion. Section 6 requires the error button to call `reset()` only. The installed `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/error.md` documents `reset()` as clearing the boundary without re-fetching and `retry()` as re-fetching. The fixture remains on the error fallback after the synthetic read failure is removed. Changing the plan's explicit action or weakening the recovery assertion requires a new instruction.

No acceptance file asserts an incorrect plural for one movement. The four acceptance files remain byte-identical to `9d5162d`; there was no step 4 acceptance stop.

## 3. Every edit to a pre-existing test

### New ledger rows for the owner-authorized follow-up

These rows compare the owner's starting commit `a80810e` with the current files. Each is tagged `plan-changed` or `stale on main (fails at c619ac6)`. The latter tag refers to the complete failing spec journey recorded in the baseline tables; it does not claim that every downstream assertion ran on main. Locations are final file line numbers. `⏎` denotes an original line break. Fixture application mirrors are implementation files, not test edits.

| File and final line | Authorization | Old → new |
| --- | --- | --- |
| `tests/authenticated-e2e/animal-surface-pilot-authenticated.spec.ts:167` | `stale on main (fails at c619ac6)` | <code>await page.getByRole(&quot;button&quot;, { name: &quot;Start with example&quot; }).click(); ⏎ await expect(page.getByRole(&quot;heading&quot;, { name: &quot;Choose a training day&quot; })).toBeVisible();</code> → <code>await page.getByRole(&quot;radio&quot;, { name: /Example routine/ }).check(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Continue&quot;, exact: true }).click(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Continue&quot;, exact: true }).click(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Save routine&quot;, exact: true }).click(); ⏎ await expect(page.getByRole(&quot;heading&quot;, { name: &quot;All days&quot; })).toBeVisible();</code> |
| `tests/authenticated-e2e/animal-surface-pilot-authenticated.spec.ts:189` | `stale on main (fails at c619ac6)` | <code>await page.getByRole(&quot;link&quot;, { name: /Open Push to start/u }).click();</code> → <code>await page.getByRole(&quot;link&quot;, { name: /Push/ }).click();</code> |
| `tests/authenticated-e2e/animal-surface-pilot-authenticated.spec.ts:195` | `stale on main (fails at c619ac6)` | <code>await page.getByRole(&quot;button&quot;, { name: &quot;Start or resume workout&quot; }).click();</code> → <code>await page.getByRole(&quot;button&quot;, { name: &quot;Start workout&quot; }).click();</code> |
| `tests/authenticated-e2e/animal-surface-pilot-authenticated.spec.ts:269` | `stale on main (fails at c619ac6)` | <code>await page.getByRole(&quot;button&quot;, { name: &quot;Start with example&quot; }).click(); ⏎ await expect(page.getByRole(&quot;heading&quot;, { name: &quot;Choose a training day&quot; })).toBeVisible();</code> → <code>await page.getByRole(&quot;radio&quot;, { name: /Example routine/ }).check(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Continue&quot;, exact: true }).click(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Continue&quot;, exact: true }).click(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Save routine&quot;, exact: true }).click(); ⏎ await expect(page.getByRole(&quot;heading&quot;, { name: &quot;All days&quot; })).toBeVisible();</code> |
| `tests/authenticated-e2e/animal-surface-pilot-authenticated.spec.ts:331` | `stale on main (fails at c619ac6)` | <code>await page.getByRole(&quot;button&quot;, { name: &quot;Start with example&quot; }).click(); ⏎ await expect(page.getByRole(&quot;heading&quot;, { name: &quot;Choose a training day&quot; })).toBeVisible();</code> → <code>await page.getByRole(&quot;radio&quot;, { name: /Example routine/ }).check(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Continue&quot;, exact: true }).click(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Continue&quot;, exact: true }).click(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Save routine&quot;, exact: true }).click(); ⏎ await expect(page.getByRole(&quot;heading&quot;, { name: &quot;All days&quot; })).toBeVisible();</code> |
| `tests/authenticated-e2e/animal-surface-pilot-authenticated.spec.ts:342` | `stale on main (fails at c619ac6)` | <code>const loading = page.locator(&#x27;.member-state[role=&quot;status&quot;]&#x27;); ⏎ await expect(loading).toContainText(&quot;Loading your home…&quot;);</code> → <code>const loading = page.locator(&#x27;.member-state[aria-busy=&quot;true&quot;]&#x27;); ⏎ await expect(loading).toContainText(&quot;Loading…&quot;);</code> |
| `tests/authenticated-e2e/animal-surface-pilot-authenticated.spec.ts:347` | `stale on main (fails at c619ac6)` | <code>await expect(page.getByRole(&quot;heading&quot;, { name: /Welcome back/u })).toBeVisible();</code> → <code>await expect(page.getByRole(&quot;heading&quot;, { name: /Ready when you are/u })).toBeVisible();</code> |
| `tests/authenticated-e2e/animal-surface-pilot-authenticated.spec.ts:356` | `stale on main (fails at c619ac6)` | <code>await expect(error).toContainText(&quot;Your home did not load.&quot;); ⏎ await expect(error).toContainText(&quot;No routine or workout changes were made.&quot;);</code> → <code>await expect(error).toContainText(&quot;This page didn&#x27;t load&quot;); ⏎ await expect(error).toContainText(&quot;Nothing was changed.&quot;);</code> |
| `tests/authenticated-e2e/animal-surface-pilot-authenticated.spec.ts:361` | `stale on main (fails at c619ac6)` | <code>await expect(page.getByRole(&quot;heading&quot;, { name: /Welcome back/u })).toBeVisible();</code> → <code>await expect(page.getByRole(&quot;heading&quot;, { name: /Ready when you are/u })).toBeVisible();</code> |
| `tests/authenticated-e2e/animal-surface-rollout-authenticated.spec.ts:211` | `stale on main (fails at c619ac6)` | <code>await page.getByRole(&quot;button&quot;, { name: &quot;Start with example&quot; }).click(); ⏎ await expect(page.getByRole(&quot;heading&quot;, { name: &quot;Choose a training day&quot; })).toBeVisible();</code> → <code>await page.getByRole(&quot;radio&quot;, { name: /Example routine/ }).check(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Continue&quot;, exact: true }).click(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Continue&quot;, exact: true }).click(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Save routine&quot;, exact: true }).click(); ⏎ await expect(page.getByRole(&quot;heading&quot;, { name: &quot;All days&quot; })).toBeVisible();</code> |
| `tests/authenticated-e2e/animal-surface-rollout-authenticated.spec.ts:225` | `stale on main (fails at c619ac6)` | <code>await expectCompanion(page, &quot;library&quot;, width &gt;= 1024, [</code> → <code>await expectCompanion(page, &quot;library&quot;, true, [</code> |
| `tests/authenticated-e2e/animal-surface-rollout-authenticated.spec.ts:244` | `stale on main (fails at c619ac6)` | <code>await expect(page.getByRole(&quot;heading&quot;, { name: &quot;Edit your route&quot; })).toBeVisible();</code> → <code>await expect(page.getByRole(&quot;heading&quot;, { name: &quot;Your routine&quot; })).toBeVisible();</code> |
| `tests/authenticated-e2e/animal-surface-rollout-authenticated.spec.ts:298` | `stale on main (fails at c619ac6)` | <code>await page.getByLabel(&quot;Program name&quot;).fill(&quot;Draft state hides decoration&quot;);</code> → <code>await page.getByLabel(&quot;Routine name&quot;).fill(&quot;Draft state hides decoration&quot;);</code> |
| `tests/authenticated-e2e/animal-surface-rollout-authenticated.spec.ts:300` | `stale on main (fails at c619ac6)` | <code>await expect(page.getByText(&quot;Unpublished changes&quot;)).toBeVisible();</code> → <code>await expect(page.getByText(&quot;Unsaved changes&quot;, { exact: true })).toBeVisible();</code> |
| `tests/authenticated-e2e/animal-surface-rollout-authenticated.spec.ts:320` | `stale on main (fails at c619ac6)` | <code>await expect(page.getByText(&quot;Preferences saved.&quot;)).toBeVisible();</code> → <code>await expect(page.getByText(&quot;Saved.&quot;, { exact: true })).toBeVisible();</code> |
| `tests/authenticated-e2e/animal-surface-rollout-authenticated.spec.ts:324` | `stale on main (fails at c619ac6)` | <code>await page.getByRole(&quot;link&quot;, { name: /Open Push to start/u }).click();</code> → <code>await page.getByRole(&quot;link&quot;, { name: /Push/ }).click();</code> |
| `tests/authenticated-e2e/animal-surface-rollout-authenticated.spec.ts:330` | `stale on main (fails at c619ac6)` | <code>await page.getByRole(&quot;button&quot;, { name: &quot;Start or resume workout&quot; }).click();</code> → <code>await page.getByRole(&quot;button&quot;, { name: &quot;Start workout&quot; }).click();</code> |
| `tests/authenticated-e2e/animal-surface-rollout-authenticated.spec.ts:364` | `stale on main (fails at c619ac6)` | <code>await expect(page.getByRole(&quot;heading&quot;, { name: &quot;Offline queued&quot; })).toBeVisible();</code> → <code>await expect(page.getByRole(&quot;heading&quot;, { name: &quot;You&#x27;re offline&quot; })).toBeVisible();</code> |
| `tests/authenticated-e2e/animal-surface-rollout-authenticated.spec.ts:367` | `stale on main (fails at c619ac6)` | <code>await expect(page.getByRole(&quot;heading&quot;, { name: &quot;Offline queued&quot; })).toHaveCount(0);</code> → <code>await expect(page.getByRole(&quot;heading&quot;, { name: &quot;You&#x27;re offline&quot; })).toHaveCount(0);</code> |
| `tests/authenticated-e2e/animal-surface-rollout-authenticated.spec.ts:395` | `stale on main (fails at c619ac6)` | <code>await page.getByLabel(&quot;Duration (seconds)&quot;).last().fill(&quot;1200&quot;);</code> → <code>await page.getByLabel(&quot;Duration&quot;, { exact: true }).fill(&quot;20:00&quot;);</code> |
| `tests/authenticated-e2e/animal-surface-rollout-authenticated.spec.ts:415` | `stale on main (fails at c619ac6)` | <code>await page.getByRole(&quot;button&quot;, { name: &quot;Skip exercise&quot; }).click();</code> → <code>if (await page.locator(&quot;.runner-more&quot;).getAttribute(&quot;open&quot;) === null) { ⏎ await page.getByText(&quot;More options&quot;, { exact: true }).click(); ⏎ } ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Skip exercise&quot;, exact: true }).click(); ⏎ await page.getByRole(&quot;dialog&quot;).getByRole(&quot;button&quot;, { name: &quot;Skip exercise&quot;, exact: true }).click();</code> |
| `tests/authenticated-e2e/animal-surface-rollout-authenticated.spec.ts:430` | `stale on main (fails at c619ac6)` | <code>await page.getByRole(&quot;button&quot;, { name: &quot;Complete workout&quot; }).click();</code> → <code>await page.getByRole(&quot;button&quot;, { name: &quot;Finish workout&quot; }).click();</code> |
| `tests/authenticated-e2e/animal-surface-rollout-authenticated.spec.ts:490` | `stale on main (fails at c619ac6)` | <code>await page.getByRole(&quot;button&quot;, { name: &quot;Start with example&quot; }).click(); ⏎ await expect(page.getByRole(&quot;heading&quot;, { name: &quot;Choose a training day&quot; })).toBeVisible();</code> → <code>await page.getByRole(&quot;radio&quot;, { name: /Example routine/ }).check(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Continue&quot;, exact: true }).click(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Continue&quot;, exact: true }).click(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Save routine&quot;, exact: true }).click(); ⏎ await expect(page.getByRole(&quot;heading&quot;, { name: &quot;All days&quot; })).toBeVisible();</code> |
| `tests/authenticated-e2e/animal-surface-rollout-authenticated.spec.ts:502` | `stale on main (fails at c619ac6)` | <code>(absent)</code> → <code>await placement.locator(&quot;img&quot;).evaluate((image) =&gt; image.dispatchEvent(new Event(&quot;error&quot;)));</code> |
| `tests/authenticated-e2e/animal-surface-rollout-authenticated.spec.ts:531` | `stale on main (fails at c619ac6)` | <code>await page.getByRole(&quot;button&quot;, { name: &quot;Start with example&quot; }).click(); ⏎ await expect(page.getByRole(&quot;heading&quot;, { name: &quot;Choose a training day&quot; })).toBeVisible(); ⏎ await page.getByRole(&quot;link&quot;, { name: /Open Push to start/u }).click();</code> → <code>await page.getByRole(&quot;radio&quot;, { name: /Example routine/ }).check(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Continue&quot;, exact: true }).click(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Continue&quot;, exact: true }).click(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Save routine&quot;, exact: true }).click(); ⏎ await expect(page.getByRole(&quot;heading&quot;, { name: &quot;All days&quot; })).toBeVisible(); ⏎ await page.getByRole(&quot;link&quot;, { name: /Push/ }).click();</code> |
| `tests/authenticated-e2e/animal-surface-rollout-authenticated.spec.ts:542` | `stale on main (fails at c619ac6)` | <code>await page.getByRole(&quot;button&quot;, { name: &quot;Start or resume workout&quot; }).click();</code> → <code>await page.getByRole(&quot;button&quot;, { name: &quot;Start workout&quot; }).click();</code> |
| `tests/authenticated-e2e/animal-surface-rollout-authenticated.spec.ts:550` | `stale on main (fails at c619ac6)` | <code>await page.getByLabel(&quot;Duration (seconds)&quot;).last().fill(&quot;1200&quot;);</code> → <code>await page.getByLabel(&quot;Duration&quot;, { exact: true }).fill(&quot;20:00&quot;);</code> |
| `tests/authenticated-e2e/animal-surface-rollout-authenticated.spec.ts:570` | `stale on main (fails at c619ac6)` | <code>await page.getByRole(&quot;button&quot;, { name: &quot;Skip exercise&quot; }).click();</code> → <code>if (await page.locator(&quot;.runner-more&quot;).getAttribute(&quot;open&quot;) === null) { ⏎ await page.getByText(&quot;More options&quot;, { exact: true }).click(); ⏎ } ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Skip exercise&quot;, exact: true }).click(); ⏎ await page.getByRole(&quot;dialog&quot;).getByRole(&quot;button&quot;, { name: &quot;Skip exercise&quot;, exact: true }).click();</code> |
| `tests/authenticated-e2e/animal-surface-rollout-authenticated.spec.ts:584` | `stale on main (fails at c619ac6)` | <code>await page.getByRole(&quot;button&quot;, { name: &quot;Complete workout&quot; }).click();</code> → <code>await page.getByRole(&quot;button&quot;, { name: &quot;Finish workout&quot; }).click();</code> |
| `tests/authenticated-e2e/customization-geometry.spec.ts:73` | `stale on main (fails at c619ac6)` | <code>const sizes = await page.locator(&quot;.member-nav a&quot;).evaluateAll((links) =&gt;</code> → <code>const sizes = await page.locator(&quot;.member-nav a, .quiet-settings-link&quot;).evaluateAll((links) =&gt;</code> |
| `tests/authenticated-e2e/customization-geometry.spec.ts:169` | `stale on main (fails at c619ac6)` | <code>await expect(page.getByRole(&quot;heading&quot;, { name: &quot;Start with the example or start blank&quot; })).toBeVisible(); ⏎ await page.getByLabel(&quot;Time zone&quot;).fill(&quot;America/Chicago&quot;);</code> → <code>await expect(page.getByRole(&quot;heading&quot;, { name: &quot;Make room for your routine.&quot; })).toBeVisible(); ⏎ await page.getByRole(&quot;radio&quot;, { name: /Example routine/ }).check(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Continue&quot;, exact: true }).click(); ⏎ await page.getByLabel(&quot;Time zone&quot;).selectOption(&quot;America/Chicago&quot;); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Continue&quot;, exact: true }).click();</code> |
| `tests/authenticated-e2e/customization-geometry.spec.ts:179` | `stale on main (fails at c619ac6)` | <code>await page.getByRole(&quot;button&quot;, { name: &quot;Start with example&quot; }).click();</code> → <code>await page.getByRole(&quot;button&quot;, { name: &quot;Save routine&quot;, exact: true }).click();</code> |
| `tests/authenticated-e2e/customization-geometry.spec.ts:181` | `stale on main (fails at c619ac6)` | <code>await expect(page.getByRole(&quot;heading&quot;, { name: &quot;Choose a training day&quot; })).toBeVisible();</code> → <code>await expect(page.getByRole(&quot;heading&quot;, { name: &quot;All days&quot; })).toBeVisible();</code> |
| `tests/authenticated-e2e/customization-geometry.spec.ts:190` | `stale on main (fails at c619ac6)` | <code>).toEqual({ dark: true, paper: &quot;#0b252b&quot;, reduced: true, scrollBehavior: &quot;auto&quot; });</code> → <code>).toEqual({ dark: true, paper: &quot;#142a23&quot;, reduced: true, scrollBehavior: &quot;auto&quot; });</code> |
| `tests/authenticated-e2e/customization-geometry.spec.ts:195` | `stale on main (fails at c619ac6)` | <code>await page.getByRole(&quot;link&quot;, { name: /Manage routines/ }).click(); ⏎ await expect(page.getByRole(&quot;heading&quot;, { name: &quot;Your routes&quot; })).toBeVisible(); ⏎ await expect(page.getByLabel(&quot;Program name&quot;)).toHaveAttribute(&quot;maxlength&quot;, &quot;80&quot;); ⏎ const cloneButton = page.getByRole(&quot;button&quot;, { name: &quot;Clone&quot; }).first();</code> → <code>await page.getByRole(&quot;link&quot;, { name: &quot;Routine&quot;, exact: true }).click(); ⏎ await page.getByRole(&quot;link&quot;, { name: &quot;All routines&quot;, exact: true }).click(); ⏎ await expect(page.getByRole(&quot;heading&quot;, { name: &quot;Your routines&quot; })).toBeVisible(); ⏎ await expect(page.getByLabel(&quot;Routine name&quot;)).toHaveAttribute(&quot;maxlength&quot;, &quot;80&quot;); ⏎ const cloneButton = page.getByRole(&quot;button&quot;, { name: &quot;Duplicate&quot; }).first();</code> |
| `tests/authenticated-e2e/customization-geometry.spec.ts:202` | `stale on main (fails at c619ac6)` | <code>await expect(page.getByRole(&quot;heading&quot;, { name: /Clone Five-day starter route/ })).toBeVisible(); ⏎ await expect(page.getByLabel(&quot;New program name&quot;)).toHaveAttribute(&quot;maxlength&quot;, &quot;80&quot;);</code> → <code>await expect(page.getByRole(&quot;heading&quot;, { name: /Duplicate Five-day starter route/ })).toBeVisible(); ⏎ await expect(page.getByLabel(&quot;New routine name&quot;)).toHaveAttribute(&quot;maxlength&quot;, &quot;80&quot;);</code> |
| `tests/authenticated-e2e/customization-geometry.spec.ts:210` | `stale on main (fails at c619ac6)` | <code>await expect(page.getByRole(&quot;heading&quot;, { name: &quot;Edit your route&quot; })).toBeVisible();</code> → <code>await expect(page.getByRole(&quot;heading&quot;, { name: &quot;Your routine&quot; })).toBeVisible(); ⏎ await page.getByText(&quot;Add a section&quot;, { exact: true }).click();</code> |
| `tests/authenticated-e2e/customization-geometry.spec.ts:220` | `stale on main (fails at c619ac6)` | <code>(absent)</code> → <code>await accessorySection.getByLabel(`More actions for ${accessoryName}`, { exact: true }).click();</code> |
| `tests/authenticated-e2e/customization-geometry.spec.ts:230` | `stale on main (fails at c619ac6)` | <code>await page.goto(&quot;/app&quot;);</code> → <code>await page.goto(&quot;/app/settings&quot;);</code> |
| `tests/authenticated-e2e/customization-geometry.spec.ts:259` | `stale on main (fails at c619ac6)` | <code>await expect(page.getByRole(&quot;heading&quot;, { name: &quot;Progress&quot; })).toBeVisible();</code> → <code>await expect(page.getByRole(&quot;heading&quot;, { name: &quot;Progress&quot;, exact: true })).toBeVisible();</code> |
| `tests/authenticated-e2e/firebase-auth-hydration.spec.ts:82` | `stale on main (fails at c619ac6)` | <code>await page.getByRole(&quot;button&quot;, { name: &quot;Start with example&quot; }).click(); ⏎ await expect(page.getByRole(&quot;heading&quot;, { name: &quot;Choose a training day&quot; })).toBeVisible();</code> → <code>await page.getByRole(&quot;radio&quot;, { name: /Example routine/ }).check(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Continue&quot;, exact: true }).click(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Continue&quot;, exact: true }).click(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Save routine&quot;, exact: true }).click(); ⏎ await expect(page.getByRole(&quot;heading&quot;, { name: &quot;All days&quot; })).toBeVisible();</code> |
| `tests/authenticated-e2e/firebase-auth-hydration.spec.ts:91` | `stale on main (fails at c619ac6)` | <code>page.getByText(&quot;The browser Firebase sign-in could not be found after initialization.&quot;),</code> → <code>page.getByText(&quot;Please sign in again to delete your account.&quot;),</code> |
| `tests/authenticated-e2e/firebase-auth-hydration.spec.ts:93` | `stale on main (fails at c619ac6)` | <code>await expect(page.getByRole(&quot;button&quot;, { name: &quot;Review permanent deletion&quot; })).toBeDisabled();</code> → <code>await expect(page.getByRole(&quot;button&quot;, { name: &quot;Delete my account&quot; })).toBeDisabled();</code> |
| `tests/authenticated-e2e/flexible-routine-publication.spec.ts:229` | `stale on main (fails at c619ac6)` | <code>await page.getByRole(&quot;button&quot;, { name: &quot;Start with example&quot; }).click();</code> → <code>await page.getByRole(&quot;radio&quot;, { name: /Example routine/ }).check(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Continue&quot;, exact: true }).click(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Continue&quot;, exact: true }).click(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Save routine&quot;, exact: true }).click();</code> |
| `tests/authenticated-e2e/flexible-routine-publication.spec.ts:253` | `stale on main (fails at c619ac6)` | <code>await page.getByRole(&quot;button&quot;, { name }).click();</code> → <code>if (name === &quot;Skip exercise&quot;) { ⏎ if (await page.locator(&quot;.runner-more&quot;).getAttribute(&quot;open&quot;) === null) { ⏎ await page.getByText(&quot;More options&quot;, { exact: true }).click(); ⏎ } ⏎ await page.getByRole(&quot;button&quot;, { name, exact: true }).click(); ⏎ await page.getByRole(&quot;dialog&quot;).getByRole(&quot;button&quot;, { name, exact: true }).click(); ⏎ } else { ⏎ await page.getByRole(&quot;button&quot;, { name, exact: true }).click(); ⏎ }</code> |
| `tests/authenticated-e2e/flexible-routine-publication.spec.ts:302` | `stale on main (fails at c619ac6)` | <code>await alice.page.getByRole(&quot;link&quot;, { name: /Manage routines/ }).click();</code> → <code>await alice.page.getByRole(&quot;link&quot;, { name: &quot;Routine&quot;, exact: true }).click(); ⏎ await alice.page.getByRole(&quot;link&quot;, { name: &quot;All routines&quot;, exact: true }).click();</code> |
| `tests/authenticated-e2e/flexible-routine-publication.spec.ts:305` | `stale on main (fails at c619ac6)` | <code>await alice.page.getByLabel(&quot;Program name&quot;).fill(&quot;Weekend route&quot;);</code> → <code>await alice.page.getByLabel(&quot;Routine name&quot;).fill(&quot;Weekend route&quot;);</code> |
| `tests/authenticated-e2e/flexible-routine-publication.spec.ts:314` | `stale on main (fails at c619ac6)` | <code>await alice.page.getByRole(&quot;button&quot;, { name: &quot;Publish custom routine&quot; }).click();</code> → <code>await alice.page.getByRole(&quot;button&quot;, { name: &quot;Create and use this routine&quot; }).click();</code> |
| `tests/authenticated-e2e/flexible-routine-publication.spec.ts:318` | `stale on main (fails at c619ac6)` | <code>alice.page.getByText(&quot;Weekend route · Revision 1 · Dumbbells · 1 day&quot;, {</code> → <code>alice.page.getByText(&quot;Weekend route · Dumbbells · 1 day&quot;, {</code> |
| `tests/authenticated-e2e/flexible-routine-publication.spec.ts:323` | `stale on main (fails at c619ac6)` | <code>await expect(alice.page.getByText(&quot;1 movements · no cardio&quot;)).toBeVisible();</code> → <code>await expect(alice.page.getByText(&quot;1 movement · no cardio&quot;)).toBeVisible();</code> |
| `tests/authenticated-e2e/flexible-routine-publication.spec.ts:340` | `stale on main (fails at c619ac6)` | <code>await expect(alice.page.getByText(&quot;1 movements · no cardio finish&quot;)).toBeVisible();</code> → <code>await expect(alice.page.getByText(&quot;1 movement · no cardio finish&quot;)).toBeVisible();</code> |
| `tests/authenticated-e2e/flexible-routine-publication.spec.ts:348` | `stale on main (fails at c619ac6)` | <code>await alice.page.getByRole(&quot;button&quot;, { name: &quot;Start or resume workout&quot; }).click();</code> → <code>await alice.page.getByRole(&quot;button&quot;, { name: &quot;Start workout&quot; }).click();</code> |
| `tests/authenticated-e2e/flexible-routine-publication.spec.ts:354` | `stale on main (fails at c619ac6)` | <code>const completion = submitRunnerAction(alice.page, &quot;Complete workout&quot;);</code> → <code>const completion = submitRunnerAction(alice.page, &quot;Finish workout&quot;);</code> |
| `tests/authenticated-e2e/flexible-routine-publication.spec.ts:364` | `stale on main (fails at c619ac6)` | <code>const daySetup = alice.page.getByRole(&quot;region&quot;, { ⏎ name: &quot;Name the day before choosing its first movement&quot;,</code> → <code>const daySetup = alice.page.getByRole(&quot;dialog&quot;, { ⏎ name: &quot;New day&quot;,</code> |
| `tests/authenticated-e2e/flexible-routine-publication.spec.ts:497` | `stale on main (fails at c619ac6)` | <code>await alice.page.getByRole(&quot;button&quot;, { name: &quot;Publish new revision&quot; }).click();</code> → <code>await alice.page.getByRole(&quot;button&quot;, { name: &quot;Save routine&quot; }).click();</code> |
| `tests/authenticated-e2e/flexible-routine-publication.spec.ts:499` | `stale on main (fails at c619ac6)` | <code>await expect(alice.page.getByText(/Published revision 2/)).toBeVisible();</code> → <code>await expect(alice.page.locator(&quot;.quiet-save-state&quot;)).toHaveText(&quot;Saved&quot;);</code> |
| `tests/authenticated-e2e/flexible-routine-publication.spec.ts:536` | `stale on main (fails at c619ac6)` | <code>await expect(alice.page.getByText(&quot;Revision 2 · Dumbbells · 2 days&quot;)).toBeVisible();</code> → <code>await expect(alice.page.getByText(&quot;Dumbbells · 2 days&quot;)).toBeVisible();</code> |
| `tests/authenticated-e2e/flexible-routine-publication.spec.ts:565` | `stale on main (fails at c619ac6)` | <code>await alice.page.getByRole(&quot;button&quot;, { name: &quot;Start or resume workout&quot; }).click();</code> → <code>await alice.page.getByRole(&quot;button&quot;, { name: &quot;Start workout&quot; }).click();</code> |
| `tests/authenticated-e2e/flexible-routine-publication.spec.ts:591` | `stale on main (fails at c619ac6)` | <code>await expect(equipmentPage.getByText(/Saved revision 3/)).toBeVisible();</code> → <code>await expect(equipmentPage.getByText(&quot;Equipment updated.&quot;, { exact: true })).toBeVisible();</code> |
| `tests/authenticated-e2e/flexible-routine-publication.spec.ts:625` | `stale on main (fails at c619ac6)` | <code>await alice.page.getByLabel(&quot;Duration (seconds)&quot;).last().fill(&quot;900&quot;);</code> → <code>await alice.page.getByLabel(&quot;Duration&quot;, { exact: true }).fill(&quot;15:00&quot;);</code> |
| `tests/authenticated-e2e/flexible-routine-publication.spec.ts:628` | `stale on main (fails at c619ac6)` | <code>const resumedCompletion = submitRunnerAction(alice.page, &quot;Complete workout&quot;);</code> → <code>const resumedCompletion = submitRunnerAction(alice.page, &quot;Finish workout&quot;);</code> |
| `tests/authenticated-e2e/flexible-routine-publication.spec.ts:649` | `stale on main (fails at c619ac6)` | <code>await expect(bob.page.getByRole(&quot;heading&quot;, { name: &quot;Choose a training day&quot; })).toBeVisible();</code> → <code>await expect(bob.page.getByRole(&quot;heading&quot;, { name: &quot;All days&quot; })).toBeVisible();</code> |
| `tests/authenticated-e2e/library-core-conditioning-expansion.spec.ts:193` | `stale on main (fails at c619ac6)` | <code>await page.getByRole(&quot;button&quot;, { name: &quot;Start with example&quot; }).click();</code> → <code>await page.getByRole(&quot;radio&quot;, { name: /Example routine/ }).check(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Continue&quot;, exact: true }).click(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Continue&quot;, exact: true }).click(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Save routine&quot;, exact: true }).click();</code> |
| `tests/authenticated-e2e/library-core-conditioning-expansion.spec.ts:209` | `stale on main (fails at c619ac6)` | <code>`^${escapeRegExp(name)} (?:weight reps&#124;bodyweight reps&#124;duration&#124;distance duration) ·`,</code> → <code>`^${escapeRegExp(name)} (?:Weight and reps&#124;Reps&#124;Time&#124;Distance and time) ·`,</code> |
| `tests/authenticated-e2e/library-core-conditioning-expansion.spec.ts:244` | `stale on main (fails at c619ac6)` | <code>await expect(alice.page.getByRole(&quot;heading&quot;, { name: &quot;Choose a training day&quot; })).toBeVisible();</code> → <code>await expect(alice.page.getByRole(&quot;heading&quot;, { name: &quot;All days&quot; })).toBeVisible();</code> |
| `tests/authenticated-e2e/library-core-conditioning-expansion.spec.ts:267` | `stale on main (fails at c619ac6)` | <code>await expect(clean.getByText(&quot;strength · weight reps&quot;, { exact: true })).toBeVisible();</code> → <code>await expect(clean.getByText(&quot;strength · Weight and reps&quot;, { exact: true })).toBeVisible();</code> |
| `tests/authenticated-e2e/library-core-conditioning-expansion.spec.ts:270` | `stale on main (fails at c619ac6)` | <code>await expect(crunch.getByText(&quot;strength · bodyweight reps&quot;, { exact: true })).toBeVisible();</code> → <code>await expect(crunch.getByText(&quot;strength · Reps&quot;, { exact: true })).toBeVisible();</code> |
| `tests/authenticated-e2e/library-core-conditioning-expansion.spec.ts:273` | `stale on main (fails at c619ac6)` | <code>await expect(flutter.getByText(&quot;strength · duration&quot;, { exact: true })).toBeVisible();</code> → <code>await expect(flutter.getByText(&quot;strength · Time&quot;, { exact: true })).toBeVisible();</code> |
| `tests/authenticated-e2e/library-core-conditioning-expansion.spec.ts:276` | `stale on main (fails at c619ac6)` | <code>await expect(carry.getByText(&quot;strength · distance duration&quot;, { exact: true })).toBeVisible();</code> → <code>await expect(carry.getByText(&quot;strength · Distance and time&quot;, { exact: true })).toBeVisible();</code> |
| `tests/authenticated-e2e/library-core-conditioning-expansion.spec.ts:290` | `stale on main (fails at c619ac6)` | <code>await alice.page.getByRole(&quot;button&quot;, { name: &quot;Publish new revision&quot; }).click();</code> → <code>await alice.page.getByRole(&quot;button&quot;, { name: &quot;Save routine&quot; }).click();</code> |
| `tests/authenticated-e2e/library-core-conditioning-expansion.spec.ts:318` | `stale on main (fails at c619ac6)` | <code>await alice.page.getByRole(&quot;button&quot;, { name: &quot;Publish new revision&quot; }).click();</code> → <code>await alice.page.getByRole(&quot;button&quot;, { name: &quot;Save routine&quot; }).click();</code> |
| `tests/authenticated-e2e/library-core-conditioning-expansion.spec.ts:321` | `stale on main (fails at c619ac6)` | <code>await expect(alice.page.getByText(/Published revision 2/u)).toBeVisible();</code> → <code>await expect(alice.page.locator(&quot;.quiet-save-state&quot;)).toHaveText(&quot;Saved&quot;);</code> |
| `tests/authenticated-e2e/library-core-conditioning-expansion.spec.ts:361` | `stale on main (fails at c619ac6)` | <code>await alice.page.getByRole(&quot;button&quot;, { name: &quot;Start or resume workout&quot; }).click();</code> → <code>await alice.page.getByRole(&quot;button&quot;, { name: &quot;Start workout&quot; }).click();</code> |
| `tests/authenticated-e2e/library-core-conditioning-expansion.spec.ts:366` | `stale on main (fails at c619ac6)` | <code>(absent)</code> → <code>await alice.page.getByText(&quot;Workout outline&quot;, { exact: true }).click();</code> |
| `tests/authenticated-e2e/library-core-conditioning-expansion.spec.ts:369` | `stale on main (fails at c619ac6)` | <code>(absent)</code> → <code>await alice.page.getByText(&quot;Watch demo and technique guidance&quot;, { exact: true }).click();</code> |
| `tests/authenticated-e2e/library-core-conditioning-expansion.spec.ts:374` | `stale on main (fails at c619ac6)` | <code>&quot;No approved catalog pair is available for this movement. Workout logging remains available.&quot;,</code> → <code>&quot;No demonstration is available for this movement. Workout logging remains available.&quot;,</code> |
| `tests/authenticated-e2e/library-core-conditioning-expansion.spec.ts:387` | `stale on main (fails at c619ac6)` | <code>await alice.page.getByRole(&quot;button&quot;, { name: &quot;Save set&quot; }).click();</code> → <code>await alice.page.getByRole(&quot;button&quot;, { name: /^(Log&#124;Update) set &amp; rest$/ }).click();</code> |
| `tests/authenticated-e2e/library-guidance.spec.ts:87` | `stale on main (fails at c619ac6)` | <code>await page.getByRole(&quot;button&quot;, { name: &quot;Start with example&quot; }).click();</code> → <code>await page.getByRole(&quot;radio&quot;, { name: /Example routine/ }).check(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Continue&quot;, exact: true }).click(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Continue&quot;, exact: true }).click(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Save routine&quot;, exact: true }).click();</code> |
| `tests/authenticated-e2e/library-guidance.spec.ts:92` | `stale on main (fails at c619ac6)` | <code>await expect(page.getByRole(&quot;heading&quot;, { name: &quot;Choose a training day&quot; })).toBeVisible();</code> → <code>await expect(page.getByRole(&quot;heading&quot;, { name: &quot;All days&quot; })).toBeVisible();</code> |
| `tests/authenticated-e2e/library-guidance.spec.ts:125` | `stale on main (fails at c619ac6)` | <code>await reopened.getByRole(&quot;button&quot;, { name: /Suitcase march/ }).click();</code> → <code>await reopened.locator(&quot;.movement-chooser__list&quot;).getByRole(&quot;button&quot;, { name: /Suitcase march/ }).click();</code> |
| `tests/authenticated-e2e/library-guidance.spec.ts:217` | `stale on main (fails at c619ac6)` | <code>await page.getByRole(&quot;button&quot;, { name: &quot;Start or resume workout&quot; }).click();</code> → <code>await page.getByRole(&quot;button&quot;, { name: &quot;Start workout&quot; }).click();</code> |
| `tests/authenticated-e2e/library-guidance.spec.ts:221` | `stale on main (fails at c619ac6)` | <code>(absent)</code> → <code>await page.getByText(&quot;Watch demo and technique guidance&quot;, { exact: true }).click();</code> |
| `tests/authenticated-e2e/library-guidance.spec.ts:245` | `stale on main (fails at c619ac6)` | <code>(absent)</code> → <code>await page.getByText(&quot;Watch demo and technique guidance&quot;, { exact: true }).click();</code> |
| `tests/authenticated-e2e/library-strength-expansion.spec.ts:90` | `stale on main (fails at c619ac6)` | <code>await page.getByRole(&quot;button&quot;, { name: &quot;Start with example&quot; }).click();</code> → <code>await page.getByRole(&quot;radio&quot;, { name: /Example routine/ }).check(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Continue&quot;, exact: true }).click(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Continue&quot;, exact: true }).click(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Save routine&quot;, exact: true }).click();</code> |
| `tests/authenticated-e2e/library-strength-expansion.spec.ts:95` | `stale on main (fails at c619ac6)` | <code>await expect(page.getByRole(&quot;heading&quot;, { name: &quot;Choose a training day&quot; })).toBeVisible();</code> → <code>await expect(page.getByRole(&quot;heading&quot;, { name: &quot;All days&quot; })).toBeVisible();</code> |
| `tests/authenticated-e2e/library-strength-expansion.spec.ts:136` | `stale on main (fails at c619ac6)` | <code>await page.getByRole(&quot;button&quot;, { name: &quot;Publish new revision&quot; }).click();</code> → <code>await page.getByRole(&quot;button&quot;, { name: &quot;Save routine&quot; }).click();</code> |
| `tests/authenticated-e2e/library-strength-expansion.spec.ts:138` | `stale on main (fails at c619ac6)` | <code>await expect(page.getByText(/Published revision \d+/u)).toBeVisible();</code> → <code>await expect(page.locator(&quot;.quiet-save-state&quot;)).toHaveText(&quot;Saved&quot;);</code> |
| `tests/authenticated-e2e/library-strength-expansion.spec.ts:165` | `stale on main (fails at c619ac6)` | <code>await page.getByRole(&quot;button&quot;, { name: &quot;Start or resume workout&quot; }).click();</code> → <code>await page.getByRole(&quot;button&quot;, { name: &quot;Start workout&quot; }).click();</code> |
| `tests/authenticated-e2e/library-strength-expansion.spec.ts:169` | `stale on main (fails at c619ac6)` | <code>(absent)</code> → <code>await page.getByText(&quot;Workout outline&quot;, { exact: true }).click();</code> |
| `tests/authenticated-e2e/library-strength-expansion.spec.ts:174` | `stale on main (fails at c619ac6)` | <code>const techniquePanel = page.locator(&quot;section.runner-technique&quot;);</code> → <code>await page.getByText(&quot;Watch demo and technique guidance&quot;, { exact: true }).click(); ⏎ const techniquePanel = page.locator(&quot;details.runner-technique&quot;);</code> |
| `tests/authenticated-e2e/library-strength-expansion.spec.ts:179` | `stale on main (fails at c619ac6)` | <code>&quot;No approved catalog pair is available for this movement. Workout logging remains available.&quot;,</code> → <code>&quot;No demonstration is available for this movement. Workout logging remains available.&quot;,</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:256` | `stale on main (fails at c619ac6)` | <code>(absent)</code> → <code>if (await page.getByRole(&quot;radio&quot;, { name: /Example routine/ }).isVisible()) { ⏎ await page.getByRole(&quot;radio&quot;, { name: mode === &quot;example&quot; ? /Example routine/ : /Blank routine/ }).check(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Continue&quot;, exact: true }).click(); ⏎ } ⏎ if (await page.getByLabel(&quot;Display units&quot;).isVisible()) { ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Continue&quot;, exact: true }).click(); ⏎ }</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:266` | `stale on main (fails at c619ac6)` | <code>if (mode === &quot;blank&quot;) { ⏎ await page.getByRole(&quot;radio&quot;, { name: /Blank routine/ }).check();</code> → <code>if (mode === &quot;blank&quot; &amp;&amp; await page.getByLabel(&quot;Search movements&quot;).isVisible()) { ⏎ await page.getByLabel(&quot;Search movements&quot;).fill(&quot;Dead bug&quot;); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Dead bug bodyweight&quot;, exact: true }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:276` | `stale on main (fails at c619ac6)` | <code>name: mode === &quot;example&quot; ? &quot;Start with example&quot; : &quot;Start blank&quot;,</code> → <code>name: &quot;Save routine&quot;, exact: true,</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:289` | `stale on main (fails at c619ac6)` | <code>await chooser.getByRole(&quot;button&quot;, { name }).click();</code> → <code>await chooser.locator(&quot;.movement-chooser__list&quot;).getByRole(&quot;button&quot;, { name }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:393` | `stale on main (fails at c619ac6)` | <code>const saveButton = page.getByRole(&quot;button&quot;, { name: &quot;Save set&quot; });</code> → <code>const saveButton = page.getByRole(&quot;button&quot;, { name: /^(Log&#124;Update) set &amp; rest$/ });</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:410` | `stale on main (fails at c619ac6)` | <code>await page.getByRole(&quot;button&quot;, { name }).click();</code> → <code>if (name === &quot;Skip exercise&quot;) { ⏎ if (await page.locator(&quot;.runner-more&quot;).getAttribute(&quot;open&quot;) === null) { ⏎ await page.getByText(&quot;More options&quot;, { exact: true }).click(); ⏎ } ⏎ await page.getByRole(&quot;button&quot;, { name, exact: true }).click(); ⏎ await page.getByRole(&quot;dialog&quot;).getByRole(&quot;button&quot;, { name, exact: true }).click(); ⏎ } else { ⏎ await page.getByRole(&quot;button&quot;, { name, exact: true }).click(); ⏎ }</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:434` | `stale on main (fails at c619ac6)` | <code>await page.getByRole(&quot;button&quot;, { name: &quot;Start or resume workout&quot; }).click();</code> → <code>await page.getByRole(&quot;button&quot;, { name: &quot;Start workout&quot; }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:448` | `stale on main (fails at c619ac6)` | <code>expect((await submitRunnerAction(page, &quot;Complete exercise&quot;)).status()).toBe(200);</code> → <code>expect((await submitRunnerAction(page, &quot;Next exercise&quot;)).status()).toBe(200); ⏎ await page.getByText(&quot;Workout outline&quot;, { exact: true }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:463` | `stale on main (fails at c619ac6)` | <code>await page.getByLabel(&quot;Duration (seconds)&quot;).last().fill(&quot;1200&quot;);</code> → <code>await page.getByLabel(&quot;Duration&quot;, { exact: true }).fill(&quot;20:00&quot;);</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:468` | `stale on main (fails at c619ac6)` | <code>const completionPromise = submitRunnerAction(page, &quot;Complete workout&quot;);</code> → <code>const completionPromise = submitRunnerAction(page, &quot;Finish workout&quot;);</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:499` | `stale on main (fails at c619ac6)` | <code>await expect(blank.page.getByRole(&quot;heading&quot;, { name: &quot;Edit your route&quot; })).toBeVisible(); ⏎ await expect(blank.page.getByLabel(&quot;Program name&quot;)).toHaveValue(&quot;Blank routine&quot;);</code> → <code>await expect(blank.page.getByRole(&quot;heading&quot;, { name: &quot;Your routine&quot; })).toBeVisible(); ⏎ await expect(blank.page.getByLabel(&quot;Routine name&quot;)).toHaveValue(&quot;Blank routine&quot;);</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:550` | `stale on main (fails at c619ac6)` | <code>await expect(unverified.page.getByText(&quot;Read-only account.&quot;)).toBeVisible(); ⏎ await expect(unverified.page.getByRole(&quot;button&quot;, { name: &quot;Start with example&quot; })).toBeDisabled();</code> → <code>await expect(unverified.page.getByText(&quot;Verify your email to save changes.&quot;)).toBeVisible(); ⏎ await expect(unverified.page.getByRole(&quot;button&quot;, { name: &quot;Continue&quot;, exact: true })).toBeDisabled();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:568` | `stale on main (fails at c619ac6)` | <code>await expect(alice.page.getByRole(&quot;heading&quot;, { name: &quot;Choose a training day&quot; })).toBeVisible(); ⏎ await expect(alice.page.getByRole(&quot;heading&quot;, { name: &quot;Welcome back, Alice QA&quot; })).toBeVisible();</code> → <code>await expect(alice.page.getByRole(&quot;heading&quot;, { name: &quot;All days&quot; })).toBeVisible(); ⏎ await expect(alice.page.getByRole(&quot;heading&quot;, { name: &quot;Ready when you are, Alice QA.&quot; })).toBeVisible();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:579` | `stale on main (fails at c619ac6)` | <code>await expect(alice.page.getByText(&quot;Revision 1 · Dumbbells · 5 days&quot;)).toBeVisible();</code> → <code>await expect(alice.page.getByText(&quot;Dumbbells · 5 days&quot;)).toBeVisible();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:584` | `stale on main (fails at c619ac6)` | <code>await alice.page.getByRole(&quot;link&quot;, { name: /Open Push to start/ }).click();</code> → <code>await alice.page.getByRole(&quot;link&quot;, { name: /Push/ }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:590` | `stale on main (fails at c619ac6)` | <code>await alice.page.getByRole(&quot;button&quot;, { name: &quot;Start or resume workout&quot; }).click();</code> → <code>await alice.page.getByRole(&quot;button&quot;, { name: &quot;Start workout&quot; }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:643` | `stale on main (fails at c619ac6)` | <code>await expect(bob.page.getByText(&quot;Revision 1 · Barbell + rack · 5 days&quot;)).toBeVisible();</code> → <code>await expect(bob.page.getByText(&quot;Barbell + rack · 5 days&quot;)).toBeVisible();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:674` | `stale on main (fails at c619ac6)` | <code>await expect(bob.page.getByRole(&quot;heading&quot;, { name: &quot;Choose a training day&quot; })).toBeVisible();</code> → <code>await expect(bob.page.getByRole(&quot;heading&quot;, { name: &quot;All days&quot; })).toBeVisible();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:727` | `stale on main (fails at c619ac6)` | <code>await expect(page.getByRole(&quot;heading&quot;, { name: &quot;Start with the example or start blank&quot; })).toBeVisible();</code> → <code>await expect(page.getByRole(&quot;heading&quot;, { name: &quot;Make room for your routine.&quot; })).toBeVisible();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:734` | `stale on main (fails at c619ac6)` | <code>await expect(page.getByRole(&quot;heading&quot;, { name: &quot;Choose a training day&quot; })).toBeVisible();</code> → <code>await expect(page.getByRole(&quot;heading&quot;, { name: &quot;All days&quot; })).toBeVisible();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:749` | `stale on main (fails at c619ac6)` | <code>await alice.page.getByRole(&quot;link&quot;, { name: /Edit routine/ }).click();</code> → <code>await alice.page.getByRole(&quot;link&quot;, { name: &quot;Routine&quot;, exact: true }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:755` | `stale on main (fails at c619ac6)` | <code>await expect(alice.page.getByText(&quot;Unpublished changes&quot;)).toBeVisible();</code> → <code>await expect(alice.page.getByText(&quot;Unsaved changes&quot;, { exact: true })).toBeVisible();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:757` | `stale on main (fails at c619ac6)` | <code>(absent)</code> → <code>await alice.page.getByText(&quot;Equipment and substitutions&quot;, { exact: true }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:769` | `stale on main (fails at c619ac6)` | <code>alice.page.getByText(/Your unpublished editor changes are not included/),</code> → <code>alice.page.getByText(&quot;Save or discard your routine edits first.&quot;),</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:780` | `stale on main (fails at c619ac6)` | <code>expect(dialog.message()).toBe(&quot;Discard this unpublished program draft?&quot;);</code> → <code>expect(dialog.message()).toBe(&quot;Discard your routine changes?&quot;);</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:791` | `stale on main (fails at c619ac6)` | <code>await expect(alice.page.getByText(&quot;Unpublished changes&quot;)).toBeVisible();</code> → <code>await expect(alice.page.getByText(&quot;Unsaved changes&quot;, { exact: true })).toBeVisible();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:800` | `stale on main (fails at c619ac6)` | <code>await expect(alice.page.getByRole(&quot;heading&quot;, { name: &quot;Choose a training day&quot; })).toBeVisible();</code> → <code>await expect(alice.page.getByRole(&quot;heading&quot;, { name: &quot;All days&quot; })).toBeVisible();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:823` | `stale on main (fails at c619ac6)` | <code>await alice.page.getByLabel(&quot;Time zone&quot;).fill(&quot;UTC&quot;);</code> → <code>await alice.page.getByRole(&quot;radio&quot;, { name: /Example routine/ }).check(); ⏎ await alice.page.getByRole(&quot;button&quot;, { name: &quot;Continue&quot;, exact: true }).click(); ⏎ await alice.page.getByLabel(&quot;Time zone&quot;).selectOption(&quot;UTC&quot;);</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:830` | `stale on main (fails at c619ac6)` | <code>await alice.page.getByRole(&quot;link&quot;, { name: &quot;Home&quot;, exact: true }).click(); ⏎ await alice.page.getByRole(&quot;link&quot;, { name: /Manage routines/ }).click(); ⏎ await alice.page.getByLabel(&quot;Program name&quot;).fill(&quot;QA barbell route&quot;);</code> → <code>await alice.page.getByRole(&quot;link&quot;, { name: &quot;Today&quot;, exact: true }).click(); ⏎ await alice.page.getByRole(&quot;link&quot;, { name: &quot;Routine&quot;, exact: true }).click(); ⏎ await alice.page.getByRole(&quot;link&quot;, { name: &quot;All routines&quot;, exact: true }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:834` | `stale on main (fails at c619ac6)` | <code>(absent)</code> → <code>await alice.page.getByLabel(&quot;Routine name&quot;).fill(&quot;QA barbell route&quot;);</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:840` | `stale on main (fails at c619ac6)` | <code>await alice.page.getByRole(&quot;button&quot;, { name: &quot;Create from example&quot; }).click();</code> → <code>await alice.page.getByRole(&quot;button&quot;, { name: &quot;Create and use this routine&quot; }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:842` | `stale on main (fails at c619ac6)` | <code>await expect(alice.page.getByText(&quot;Revision 1 · Barbell + rack · 5 days&quot;)).toBeVisible();</code> → <code>await expect(alice.page.getByText(&quot;Barbell + rack · 5 days&quot;)).toBeVisible();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:845` | `stale on main (fails at c619ac6)` | <code>await alice.page.getByRole(&quot;link&quot;, { name: /Manage routines/ }).click();</code> → <code>await alice.page.getByRole(&quot;link&quot;, { name: &quot;Routine&quot;, exact: true }).click(); ⏎ await alice.page.getByRole(&quot;link&quot;, { name: &quot;All routines&quot;, exact: true }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:850` | `stale on main (fails at c619ac6)` | <code>await barbellProgramCard.getByRole(&quot;button&quot;, { name: &quot;Clone&quot; }).click(); ⏎ await alice.page.getByLabel(&quot;New program name&quot;).fill(&quot;QA cloned route&quot;);</code> → <code>await barbellProgramCard.getByRole(&quot;button&quot;, { name: &quot;Duplicate&quot; }).click(); ⏎ await alice.page.getByLabel(&quot;New routine name&quot;).fill(&quot;QA cloned route&quot;);</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:857` | `stale on main (fails at c619ac6)` | <code>await alice.page.getByRole(&quot;button&quot;, { name: &quot;Clone and activate&quot; }).click();</code> → <code>await alice.page.getByRole(&quot;dialog&quot;).getByRole(&quot;button&quot;, { name: &quot;Duplicate&quot;, exact: true }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:859` | `stale on main (fails at c619ac6)` | <code>(absent)</code> → <code>await alice.page.getByRole(&quot;link&quot;, { name: &quot;Back to Today&quot;, exact: true }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:861` | `stale on main (fails at c619ac6)` | <code>alice.page.getByText(&quot;QA cloned route · Revision 1 · Barbell + rack · 5 days&quot;, {</code> → <code>alice.page.getByText(&quot;QA cloned route · Barbell + rack · 5 days&quot;, {</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:873` | `stale on main (fails at c619ac6)` | <code>await alice.page.getByRole(&quot;link&quot;, { name: /Manage routines/ }).click();</code> → <code>await alice.page.getByRole(&quot;link&quot;, { name: &quot;Routine&quot;, exact: true }).click(); ⏎ await alice.page.getByRole(&quot;link&quot;, { name: &quot;All routines&quot;, exact: true }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:888` | `stale on main (fails at c619ac6)` | <code>await expect(alice.page.getByText(&quot;Revision 1 · Dumbbells · 5 days&quot;)).toBeVisible();</code> → <code>await expect(alice.page.getByText(&quot;Dumbbells · 5 days&quot;)).toBeVisible();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:890` | `stale on main (fails at c619ac6)` | <code>await alice.page.getByRole(&quot;link&quot;, { name: /Manage private exercises/ }).click(); ⏎ await alice.page.getByRole(&quot;link&quot;, { name: &quot;Create exercise&quot; }).click();</code> → <code>await alice.page.getByRole(&quot;link&quot;, { name: &quot;Library&quot;, exact: true }).click(); ⏎ await alice.page.getByRole(&quot;link&quot;, { name: &quot;Create private exercise&quot; }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:913` | `stale on main (fails at c619ac6)` | <code>await alice.page.getByRole(&quot;link&quot;, { name: &quot;Home&quot;, exact: true }).click(); ⏎ await alice.page.getByRole(&quot;link&quot;, { name: /Edit routine/ }).click();</code> → <code>await alice.page.getByRole(&quot;link&quot;, { name: &quot;Today&quot;, exact: true }).click(); ⏎ await alice.page.getByRole(&quot;link&quot;, { name: &quot;Routine&quot;, exact: true }).click(); ⏎ await alice.page.getByText(&quot;Add a section&quot;, { exact: true }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:924` | `stale on main (fails at c619ac6)` | <code>(absent)</code> → <code>await accessorySection.getByLabel(`More actions for ${accessoryName}`, { exact: true }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:964` | `stale on main (fails at c619ac6)` | <code>(absent)</code> → <code>await customPrescription.getByLabel(&quot;More actions for QA supported row&quot;, { exact: true }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:972` | `stale on main (fails at c619ac6)` | <code>(absent)</code> → <code>await newAccessorySection.getByLabel(&quot;More actions for Custom assistance&quot;, { exact: true }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:978` | `stale on main (fails at c619ac6)` | <code>await walkerEditor.getByLabel(&quot;Duration seconds&quot;).fill(&quot;900&quot;);</code> → <code>await walkerEditor.getByLabel(&quot;Duration&quot;, { exact: true }).fill(&quot;15:00&quot;);</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:1005` | `stale on main (fails at c619ac6)` | <code>await alice.page.getByRole(&quot;button&quot;, { name: &quot;Publish new revision&quot; }).click();</code> → <code>await alice.page.getByRole(&quot;button&quot;, { name: &quot;Save routine&quot; }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:1012` | `stale on main (fails at c619ac6)` | <code>alice.page.getByText(&quot;The synthetic transport lost the accepted publication response.&quot;),</code> → <code>alice.page.getByText(&quot;Couldn&#x27;t save your routine. Try again.&quot;),</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:1025` | `stale on main (fails at c619ac6)` | <code>await alice.page.getByRole(&quot;button&quot;, { name: &quot;Publish new revision&quot; }).click();</code> → <code>await alice.page.getByRole(&quot;button&quot;, { name: &quot;Save routine&quot; }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:1030` | `stale on main (fails at c619ac6)` | <code>await expect(alice.page.getByText(/Published revision 2/)).toBeVisible(); ⏎ await expect(alice.page.getByText(&quot;Draft matches the active revision&quot;)).toBeVisible();</code> → <code>await expect(alice.page.getByText(&quot;Routine saved. Past workouts stay as they were.&quot;, { exact: true })).toBeVisible(); ⏎ await expect(alice.page.locator(&quot;.quiet-save-state&quot;)).toHaveText(&quot;Saved&quot;);</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:1046` | `stale on main (fails at c619ac6)` | <code>await alice.page.getByRole(&quot;link&quot;, { name: &quot;Back to program&quot; }).click(); ⏎ await expect(alice.page.getByRole(&quot;heading&quot;, { name: &quot;Welcome back, Alice QA&quot; })).toBeVisible();</code> → <code>await alice.page.getByRole(&quot;link&quot;, { name: &quot;Back to Today&quot; }).click(); ⏎ await expect(alice.page.getByRole(&quot;heading&quot;, { name: &quot;Ready when you are, Alice QA.&quot; })).toBeVisible();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:1051` | `stale on main (fails at c619ac6)` | <code>await alice.page.getByRole(&quot;link&quot;, { name: &quot;Home&quot;, exact: true }).click(); ⏎ await expect(alice.page.getByRole(&quot;heading&quot;, { name: &quot;Welcome back, Alice QA&quot; })).toBeVisible();</code> → <code>await alice.page.getByRole(&quot;link&quot;, { name: &quot;Today&quot;, exact: true }).click(); ⏎ await expect(alice.page.getByRole(&quot;heading&quot;, { name: &quot;Ready when you are, Alice QA.&quot; })).toBeVisible();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:1062` | `stale on main (fails at c619ac6)` | <code>await alice.page.getByRole(&quot;link&quot;, { name: /Edit routine/ }).click();</code> → <code>await alice.page.getByRole(&quot;link&quot;, { name: &quot;Routine&quot;, exact: true }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:1064` | `stale on main (fails at c619ac6)` | <code>await expect(alice.page.getByRole(&quot;heading&quot;, { name: &quot;Edit your route&quot; })).toBeVisible();</code> → <code>await expect(alice.page.getByRole(&quot;heading&quot;, { name: &quot;Your routine&quot; })).toBeVisible();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:1068` | `stale on main (fails at c619ac6)` | <code>(absent)</code> → <code>await alice.page.getByText(&quot;Equipment and substitutions&quot;, { exact: true }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:1096` | `stale on main (fails at c619ac6)` | <code>&quot;Sets, range, rest, position, and notes stay. Movement-specific targets clear.&quot;,</code> → <code>&quot;Sets, reps, rest and notes carry over.&quot;,</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:1117` | `stale on main (fails at c619ac6)` | <code>await expect(alice.page.getByText(/Saved revision 3/)).toBeVisible();</code> → <code>await expect(alice.page.getByText(&quot;Equipment updated.&quot;, { exact: true })).toBeVisible();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:1156` | `stale on main (fails at c619ac6)` | <code>await alice.page.getByLabel(&quot;IANA time zone&quot;).fill(&quot;America/Chicago&quot;);</code> → <code>await alice.page.getByLabel(&quot;Time zone&quot;).selectOption(&quot;America/Chicago&quot;);</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:1173` | `stale on main (fails at c619ac6)` | <code>await alice.page.getByRole(&quot;link&quot;, { name: &quot;Home&quot;, exact: true }).click(); ⏎ await alice.page.getByRole(&quot;link&quot;, { name: /Edit routine/ }).click();</code> → <code>await alice.page.getByRole(&quot;link&quot;, { name: &quot;Today&quot;, exact: true }).click(); ⏎ await alice.page.getByRole(&quot;link&quot;, { name: &quot;Routine&quot;, exact: true }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:1190` | `stale on main (fails at c619ac6)` | <code>await expect(alice.page.getByText(&quot;Draft matches the active revision&quot;)).toBeVisible(); ⏎ await alice.page.getByRole(&quot;link&quot;, { name: &quot;Back to program&quot; }).click();</code> → <code>await expect(alice.page.locator(&quot;.quiet-save-state&quot;)).toHaveText(&quot;Saved&quot;); ⏎ await alice.page.getByRole(&quot;link&quot;, { name: &quot;Back to Today&quot; }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:1193` | `stale on main (fails at c619ac6)` | <code>await alice.page.getByRole(&quot;link&quot;, { name: &quot;History&quot;, exact: true }).click();</code> → <code>await alice.page.getByRole(&quot;link&quot;, { name: &quot;Review history&quot;, exact: true }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:1219` | `stale on main (fails at c619ac6)` | <code>await expect(alice.page.getByRole(&quot;heading&quot;, { name: &quot;Progress&quot; })).toBeVisible();</code> → <code>await expect(alice.page.getByRole(&quot;heading&quot;, { name: &quot;Progress&quot;, exact: true })).toBeVisible();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:1313` | `stale on main (fails at c619ac6)` | <code>await alice.page.getByRole(&quot;link&quot;, { name: /Manage private exercises/ }).click(); ⏎ await alice.page.getByRole(&quot;link&quot;, { name: &quot;Create exercise&quot; }).click();</code> → <code>await alice.page.getByRole(&quot;link&quot;, { name: &quot;Library&quot;, exact: true }).click(); ⏎ await alice.page.getByRole(&quot;link&quot;, { name: &quot;Create private exercise&quot; }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:1331` | `stale on main (fails at c619ac6)` | <code>await alice.page.getByRole(&quot;link&quot;, { name: &quot;Home&quot;, exact: true }).click(); ⏎ await alice.page.getByRole(&quot;link&quot;, { name: /Edit routine/ }).click();</code> → <code>await alice.page.getByRole(&quot;link&quot;, { name: &quot;Today&quot;, exact: true }).click(); ⏎ await alice.page.getByRole(&quot;link&quot;, { name: &quot;Routine&quot;, exact: true }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:1343` | `stale on main (fails at c619ac6)` | <code>await alice.page.getByRole(&quot;button&quot;, { name: &quot;Publish new revision&quot; }).click();</code> → <code>await alice.page.getByRole(&quot;button&quot;, { name: &quot;Save routine&quot; }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:1345` | `stale on main (fails at c619ac6)` | <code>await expect(alice.page.getByText(/Published revision 2/)).toBeVisible();</code> → <code>await expect(alice.page.locator(&quot;.quiet-save-state&quot;)).toHaveText(&quot;Saved&quot;);</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:1349` | `stale on main (fails at c619ac6)` | <code>(absent)</code> → <code>await alice.page.getByText(&quot;Equipment and substitutions&quot;, { exact: true }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:1400` | `stale on main (fails at c619ac6)` | <code>await alice.page.getByRole(&quot;button&quot;, { name: &quot;Start or resume workout&quot; }).click();</code> → <code>await alice.page.getByRole(&quot;button&quot;, { name: &quot;Start workout&quot; }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:1416` | `stale on main (fails at c619ac6)` | <code>await expect(alice.page.getByRole(&quot;heading&quot;, { name: &quot;Save activity&quot; })).toBeVisible();</code> → <code>await expect(alice.page.getByRole(&quot;heading&quot;, { name: &quot;Couldn&#x27;t save&quot; })).toBeVisible();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:1449` | `stale on main (fails at c619ac6)` | <code>await expect(alice.page.getByRole(&quot;heading&quot;, { name: &quot;Save activity&quot; })).toHaveCount(0);</code> → <code>await expect(alice.page.getByRole(&quot;heading&quot;, { name: &quot;Couldn&#x27;t save&quot; })).toHaveCount(0);</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:1456` | `stale on main (fails at c619ac6)` | <code>expect((await submitRunnerAction(alice.page, &quot;Complete exercise&quot;)).status()).toBe(200);</code> → <code>expect((await submitRunnerAction(alice.page, &quot;Next exercise&quot;)).status()).toBe(200); ⏎ await alice.page.getByText(&quot;Workout outline&quot;, { exact: true }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:1473` | `stale on main (fails at c619ac6)` | <code>await alice.page.getByLabel(&quot;Duration (seconds)&quot;).last().fill(&quot;1200&quot;);</code> → <code>await alice.page.getByLabel(&quot;Duration&quot;, { exact: true }).fill(&quot;20:00&quot;);</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:1479` | `stale on main (fails at c619ac6)` | <code>const completionPromise = submitRunnerAction(alice.page, &quot;Complete workout&quot;);</code> → <code>const completionPromise = submitRunnerAction(alice.page, &quot;Finish workout&quot;);</code> |
| `tests/authenticated-e2e/quiet-set.spec.ts:50` | `plan-changed` | <code>(absent)</code> → <code>await page.getByLabel(&quot;More actions for Push-up&quot;, { exact: true }).click();</code> |
| `tests/authenticated-e2e/quiet-set.spec.ts:72` | `plan-changed` | <code>(absent)</code> → <code>await page.getByRole(&quot;link&quot;, {name:&quot;Settings&quot;, exact:true}).click();</code> |
| `tests/authenticated-e2e/quiet-set.spec.ts:74` | `plan-changed` | <code>(absent)</code> → <code>await page.getByRole(&quot;link&quot;, {name:&quot;Today&quot;, exact:true}).click();</code> |
| `tests/authenticated-e2e/quiet-set.spec.ts:77` | `plan-changed` | <code>(absent)</code> → <code>await page.getByRole(&quot;link&quot;, {name:&quot;Settings&quot;, exact:true}).click();</code> |
| `tests/authenticated-e2e/quiet-set.spec.ts:79` | `plan-changed` | <code>(absent)</code> → <code>await page.getByRole(&quot;link&quot;, {name:&quot;Today&quot;, exact:true}).click();</code> |
| `tests/authenticated-e2e/quiet-set.spec.ts:90` | `plan-changed` | <code>await expect(page.getByRole(&quot;button&quot;, {name:&quot;Complete exercise&quot;, exact:true}).first()).toBeEnabled();</code> → <code>await expect(page.getByRole(&quot;button&quot;, {name:&quot;Finish exercise&quot;, exact:true}).first()).toBeEnabled();</code> |
| `tests/authenticated-e2e/quiet-set.spec.ts:96` | `plan-changed` | <code>await page.getByRole(&quot;button&quot;, {name:&quot;Complete exercise&quot;, exact:true}).first().click(); ⏎ await page.getByRole(&quot;button&quot;, {name:&quot;Complete workout&quot;, exact:true}).click();</code> → <code>await page.getByRole(&quot;button&quot;, {name:&quot;Finish exercise&quot;, exact:true}).first().click(); ⏎ await page.getByRole(&quot;button&quot;, {name:&quot;Finish workout&quot;, exact:true}).click();</code> |
| `tests/authenticated-e2e/quiet-set.spec.ts:163` | `plan-changed` | <code>await page.getByRole(&quot;button&quot;,{name:&quot;Complete exercise&quot;,exact:true}).first().click(); ⏎ await page.getByRole(&quot;button&quot;,{name:&quot;Complete workout&quot;,exact:true}).click();</code> → <code>await page.getByRole(&quot;button&quot;,{name:&quot;Finish exercise&quot;,exact:true}).first().click(); ⏎ await page.getByRole(&quot;button&quot;,{name:&quot;Finish workout&quot;,exact:true}).click();</code> |
| `tests/authenticated-e2e/runner-resilience.spec.ts:458` | `plan-changed` | <code>await page.getByRole(&quot;button&quot;, { name }).click();</code> → <code>if (name === &quot;Skip exercise&quot;) { ⏎ if (await page.locator(&quot;.runner-more&quot;).getAttribute(&quot;open&quot;) === null) { ⏎ await page.getByText(&quot;More options&quot;, { exact: true }).click(); ⏎ } ⏎ await page.getByRole(&quot;button&quot;, { name, exact: true }).click(); ⏎ await page.getByRole(&quot;dialog&quot;).getByRole(&quot;button&quot;, { name, exact: true }).click(); ⏎ } else { ⏎ await page.getByRole(&quot;button&quot;, { name, exact: true }).click(); ⏎ }</code> |
| `tests/authenticated-e2e/runner-resilience.spec.ts:475` | `plan-changed` | <code>await submitRunnerAction(page, &quot;Complete exercise&quot;);</code> → <code>await submitRunnerAction(page, &quot;Next exercise&quot;);</code> |
| `tests/authenticated-e2e/runner-resilience.spec.ts:492` | `plan-changed` | <code>await page.getByLabel(&quot;Duration (seconds)&quot;).last().fill(&quot;1200&quot;);</code> → <code>await page.getByLabel(&quot;Duration&quot;, { exact: true }).fill(&quot;20:00&quot;);</code> |
| `tests/authenticated-e2e/runner-resilience.spec.ts:553` | `plan-changed` | <code>harness.page.getByRole(&quot;heading&quot;, { name: &quot;Offline queued&quot; }),</code> → <code>harness.page.getByRole(&quot;heading&quot;, { name: &quot;You&#x27;re offline&quot; }),</code> |
| `tests/authenticated-e2e/runner-resilience.spec.ts:582` | `plan-changed` | <code>harness.page.getByRole(&quot;heading&quot;, { name: &quot;Offline queued&quot; }),</code> → <code>harness.page.getByRole(&quot;heading&quot;, { name: &quot;You&#x27;re offline&quot; }),</code> |
| `tests/authenticated-e2e/runner-resilience.spec.ts:690` | `plan-changed` | <code>name: &quot;Reauthenticate and return&quot;,</code> → <code>name: &quot;Sign in again&quot;,</code> |
| `tests/authenticated-e2e/runner-resilience.spec.ts:807` | `plan-changed` | <code>first.getByRole(&quot;heading&quot;, { name: &quot;Offline queued&quot; }),</code> → <code>first.getByRole(&quot;heading&quot;, { name: &quot;You&#x27;re offline&quot; }),</code> |
| `tests/authenticated-e2e/runner-resilience.spec.ts:815` | `plan-changed` | <code>first.getByRole(&quot;heading&quot;, { name: &quot;Offline queued&quot; }),</code> → <code>first.getByRole(&quot;heading&quot;, { name: &quot;You&#x27;re offline&quot; }),</code> |
| `tests/authenticated-e2e/runner-resilience.spec.ts:884` | `plan-changed` | <code>name: &quot;Choose the workout value to keep&quot;,</code> → <code>name: &quot;Pick which value to keep&quot;,</code> |
| `tests/authenticated-e2e/runner-resilience.spec.ts:890` | `plan-changed` | <code>name: /Keep 25 lb · 12 reps for Set 1 · Dumbbell bench press/u,</code> → <code>name: /Keep 25 lb · 12 reps/u,</code> |
| `tests/authenticated-e2e/runner-resilience.spec.ts:893` | `plan-changed` | <code>name: /Keep 30 lb · 12 reps for Set 1 · Dumbbell bench press/u,</code> → <code>name: /Keep 30 lb · 12 reps/u,</code> |
| `tests/authenticated-e2e/runner-resilience.spec.ts:929` | `plan-changed` | <code>await first ⏎ .getByRole(&quot;button&quot;, { name: &quot;Leave both values unresolved&quot; }) ⏎ .click();</code> → <code>await conflictHeading.focus();</code> |
| `tests/authenticated-e2e/runner-resilience.spec.ts:1149` | `plan-changed` | <code>second.getByRole(&quot;heading&quot;, { name: &quot;Choose the workout value to keep&quot; }),</code> → <code>second.getByRole(&quot;heading&quot;, { name: &quot;Pick which value to keep&quot; }),</code> |
| `tests/authenticated-e2e/runner-resilience.spec.ts:1155` | `plan-changed` | <code>(absent)</code> → <code>await second.getByText(&quot;More options&quot;, { exact: true }).click();</code> |
| `tests/authenticated-e2e/runner-resilience.spec.ts:1157` | `plan-changed` | <code>.getByLabel(/Private note for Dumbbell bench press/iu)</code> → <code>.getByLabel(&quot;Note&quot;, { exact: true })</code> |
| `tests/authenticated-e2e/runner-resilience.spec.ts:1160` | `plan-changed` | <code>await expect(second.getByRole(&quot;textbox&quot;, { name: /Private note/iu })).not.toHaveValue(</code> → <code>await expect(second.getByRole(&quot;textbox&quot;, { name: &quot;Note&quot;, exact: true })).not.toHaveValue(</code> |
| `tests/authenticated-e2e/runner-resilience.spec.ts:1183` | `plan-changed` | <code>await first.getByRole(&quot;button&quot;, { name: &quot;Complete workout&quot; }).click();</code> → <code>await first.getByRole(&quot;button&quot;, { name: &quot;Finish workout&quot; }).click();</code> |
| `tests/e2e/animal-surface-pilot.spec.ts:105` | `stale on main (fails at c619ac6)` | <code>page.getByRole(&quot;heading&quot;, { level: 1, name: &quot;Your workout. Your way.&quot; }),</code> → <code>page.getByRole(&quot;heading&quot;, { level: 1, name: &quot;A little space for your next set.&quot; }),</code> |
| `tests/e2e/animal-surface-pilot.spec.ts:109` | `stale on main (fails at c619ac6)` | <code>await expectNoHorizontalOverlap(page.locator(&quot;.landing-copy&quot;), placement);</code> → <code>await expectNoHorizontalOverlap(page.locator(&quot;.quiet-welcome-copy&quot;), placement);</code> |
| `tests/e2e/animal-surface-pilot.spec.ts:111` | `stale on main (fails at c619ac6)` | <code>await expectNoIntersection(page.locator(&quot;.landing-copy&quot;), placement);</code> → <code>await expectNoIntersection(page.locator(&quot;.quiet-welcome-copy&quot;), placement);</code> |
| `tests/e2e/animal-surface-pilot.spec.ts:125` | `stale on main (fails at c619ac6)` | <code>page.getByRole(&quot;heading&quot;, { level: 1, name: &quot;Your workout. Your way.&quot; }),</code> → <code>page.getByRole(&quot;heading&quot;, { level: 1, name: &quot;A little space for your next set.&quot; }),</code> |
| `tests/e2e/animal-surface-pilot.spec.ts:148` | `stale on main (fails at c619ac6)` | <code>copy: &quot;.landing-copy&quot;, ⏎ heading: &quot;Your workout. Your way.&quot;,</code> → <code>copy: &quot;.quiet-welcome-copy&quot;, ⏎ heading: &quot;A little space for your next set.&quot;,</code> |
| `tests/e2e/animal-surface-pilot.spec.ts:188` | `stale on main (fails at c619ac6)` | <code>await expectNoIntersection(placement, page.locator(&quot;.landing-actions&quot;));</code> → <code>await expectNoIntersection(placement, page.locator(&quot;.quiet-welcome-copy&quot;));</code> |
| `tests/e2e/animal-surface-pilot.spec.ts:191` | `stale on main (fails at c619ac6)` | <code>page.getByText(&quot;Sample data · not your history&quot;, { exact: true }),</code> → <code>page.getByText(&quot;Example data&quot;, { exact: true }),</code> |
| `tests/e2e/animal-surface-pilot.spec.ts:323` | `stale on main (fails at c619ac6)` | <code>page.locator(&quot;.landing-actions&quot;),</code> → <code>page.locator(&quot;.quiet-welcome-copy&quot;),</code> |
| `tests/e2e/animal-surface-pilot.spec.ts:349` | `stale on main (fails at c619ac6)` | <code>page.locator(&quot;.landing-actions&quot;),</code> → <code>page.locator(&quot;.quiet-welcome-copy&quot;),</code> |
| `tests/e2e/animal-surface-rollout.spec.ts:73` | `stale on main (fails at c619ac6)` | <code>if (width &lt; 1024) { ⏎ await expect(placement).toBeHidden();</code> → <code>if (width &lt;= 700) { ⏎ await expect(placement).toBeVisible();</code> |
| `tests/e2e/animal-surface-rollout.spec.ts:87` | `stale on main (fails at c619ac6)` | <code>&quot;.guest-stamp&quot;,</code> → <code>&quot;.public-library-hero p&quot;,</code> |
| `tests/e2e/public-release.spec.ts:42` | `stale on main (fails at c619ac6)` | <code>name: &quot;Your workout. Your way.&quot;,</code> → <code>name: &quot;A little space for your next set.&quot;,</code> |
| `tests/e2e/public-release.spec.ts:45` | `stale on main (fails at c619ac6)` | <code>await expect(page.locator(&#x27;[data-companion-placement=&quot;landing&quot;]&#x27;)).toBeVisible();</code> → <code>await expect(page.locator(&#x27;.quiet-studio&#x27;)).toBeVisible();</code> |
| `tests/e2e/public-release.spec.ts:47` | `stale on main (fails at c619ac6)` | <code>page.locator(&#x27;[data-companion-placement=&quot;landing&quot;] img&#x27;),</code> → <code>page.locator(&#x27;.quiet-studio img&#x27;),</code> |
| `tests/e2e/public-release.spec.ts:49` | `stale on main (fails at c619ac6)` | <code>await expect(page.getByText(&quot;Open to everyone&quot;)).toBeVisible(); ⏎ await expect(page.getByText(&quot;Sign in to make it yours&quot;)).toBeVisible();</code> → <code>await expect(page.getByText(&quot;No account needed. Practice stays temporary.&quot;)).toBeVisible(); ⏎ await expect(page.getByRole(&quot;link&quot;, { name: &quot;Create my routine&quot; })).toBeVisible();</code> |
| `tests/e2e/public-release.spec.ts:62` | `stale on main (fails at c619ac6)` | <code>await expect(page.getByText(&quot;Starter preview · not saved&quot;)).toBeVisible();</code> → <code>await expect(page.getByRole(&quot;link&quot;, { name: &quot;Sign in to save your own version&quot; })).toBeVisible();</code> |
| `tests/e2e/public-release.spec.ts:65` | `stale on main (fails at c619ac6)` | <code>page.getByRole(&quot;button&quot;, { name: `${index + 1} ${name}` }),</code> → <code>page.getByRole(&quot;link&quot;, { name: new RegExp(`^Day ${index + 1} ${name} `) }),</code> |
| `tests/e2e/public-release.spec.ts:69` | `stale on main (fails at c619ac6)` | <code>await page.getByRole(&quot;link&quot;, { name: &quot;Open Push day&quot; }).click();</code> → <code>await page.getByRole(&quot;link&quot;, { name: /^Day 1 Push / }).click();</code> |
| `tests/e2e/public-release.spec.ts:75` | `stale on main (fails at c619ac6)` | <code>await page.getByRole(&quot;link&quot;, { name: &quot;Push day&quot; }).click(); ⏎ await expect(page).toHaveURL(/\/program\/push\?equipment=dumbbells$/); ⏎ await page.getByRole(&quot;link&quot;, { name: &quot;Five-day starter example&quot; }).click(); ⏎ await expect(page).toHaveURL(/\/program\?equipment=dumbbells$/);</code> → <code>await page.getByRole(&quot;link&quot;, { name: &quot;Exercise library&quot; }).click(); ⏎ await expect(page).toHaveURL(/\/library\?equipment=dumbbells$/); ⏎ await page.getByRole(&quot;link&quot;, { name: &quot;Example&quot;, exact: true }).click(); ⏎ await expect(page).toHaveURL(/\/program$/);</code> |
| `tests/e2e/public-release.spec.ts:80` | `stale on main (fails at c619ac6)` | <code>const barbellPreview = page.getByRole(&quot;button&quot;, {</code> → <code>const barbellPreview = page.getByRole(&quot;link&quot;, {</code> |
| `tests/e2e/public-release.spec.ts:84` | `stale on main (fails at c619ac6)` | <code>await expect(barbellPreview).toHaveAttribute(&quot;aria-pressed&quot;, &quot;true&quot;); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;2 Pull&quot; }).click(); ⏎ const selectedDay = page.locator(&quot;#selected-day-sheet&quot;);</code> → <code>await expect(barbellPreview).toHaveAttribute(&quot;aria-current&quot;, &quot;true&quot;); ⏎ await page.getByRole(&quot;link&quot;, { name: /^Day 2 Pull / }).focus(); ⏎ const selectedDay = page.locator(&quot;.program-day-grid &gt; li&quot;).filter({ has: page.getByRole(&quot;heading&quot;, { name: &quot;Pull&quot;, exact: true }) });</code> |
| `tests/e2e/public-release.spec.ts:88` | `stale on main (fails at c619ac6)` | <code>selectedDay.getByRole(&quot;heading&quot;, { level: 2, name: &quot;Pull day&quot; }),</code> → <code>selectedDay.getByRole(&quot;heading&quot;, { level: 2, name: &quot;Pull&quot; }),</code> |
| `tests/e2e/public-release.spec.ts:90` | `stale on main (fails at c619ac6)` | <code>await expect(selectedDay.getByText(&quot;Barbell bent-over row&quot;)).toBeVisible();</code> → <code>await expect(selectedDay.getByText(&quot;Barbell bent-over row&quot;, { exact: true })).toBeVisible();</code> |
| `tests/e2e/public-release.spec.ts:92` | `stale on main (fails at c619ac6)` | <code>await selectedDay.getByRole(&quot;link&quot;, { name: &quot;Open Pull day&quot; }).click();</code> → <code>await selectedDay.getByRole(&quot;link&quot;, { name: /^Day 2 Pull / }).click();</code> |
| `tests/e2e/public-release.spec.ts:100` | `stale on main (fails at c619ac6)` | <code>await expect(page.getByText(&quot;Starter preview · not saved&quot;)).toBeVisible();</code> → <code>await expect(page.getByText(&quot;Edit cardio targets once you save a routine.&quot;)).toBeVisible();</code> |
| `tests/e2e/public-release.spec.ts:113` | `stale on main (fails at c619ac6)` | <code>await expect(page).toHaveURL(/\/library\?equipment=barbell&amp;q=bent\+over\+row$/);</code> → <code>await expect(page).toHaveURL(/\/library\?equipment=barbell$/);</code> |
| `tests/e2e/public-release.spec.ts:134` | `stale on main (fails at c619ac6)` | <code>page.getByRole(&quot;heading&quot;, { level: 1, name: &quot;Lower workout&quot; }),</code> → <code>page.getByRole(&quot;heading&quot;, { level: 1, name: &quot;Example workout&quot; }),</code> |
| `tests/e2e/public-release.spec.ts:143` | `stale on main (fails at c619ac6)` | <code>await expect(page.getByText(&quot;Sample data · not your history&quot;, { exact: true })).toHaveCount(1);</code> → <code>await expect(page.getByText(&quot;Example data&quot;, { exact: true })).toHaveCount(1);</code> |
| `tests/e2e/public-release.spec.ts:150` | `stale on main (fails at c619ac6)` | <code>/^(Sign-in connection pending&#124;Sign in)$/,</code> → <code>/^(Sign-in is unavailable&#124;Sign in)$/,</code> |
| `tests/e2e/public-release.spec.ts:152` | `stale on main (fails at c619ac6)` | <code>await expect(page.getByRole(&quot;link&quot;, { name: &quot;Browse the free program&quot; })).toBeVisible();</code> → <code>await expect(page.getByRole(&quot;link&quot;, { name: &quot;Explore the example routine&quot; })).toBeVisible();</code> |
| `tests/e2e/public-release.spec.ts:173` | `stale on main (fails at c619ac6)` | <code>await expect(page.getByText(&quot;Five-day example routine&quot;)).toBeVisible();</code> → <code>await expect(page.getByRole(&quot;link&quot;, { name: &quot;Sign in to save your own version&quot; })).toBeVisible();</code> |
| `tests/e2e/public-release.spec.ts:245` | `stale on main (fails at c619ac6)` | <code>.getByRole(&quot;link&quot;, { name: &quot;Explore the five-day example&quot; })</code> → <code>.getByRole(&quot;link&quot;, { name: &quot;Try one set&quot; })</code> |
| `tests/e2e/public-release.spec.ts:264` | `stale on main (fails at c619ac6)` | <code>name: &quot;Your workout. Your way.&quot;,</code> → <code>name: &quot;A little space for your next set.&quot;,</code> |
| `tests/e2e/public-release.spec.ts:276` | `stale on main (fails at c619ac6)` | <code>expect(presentation.backgroundColor).toBe(&quot;rgb(11, 37, 43)&quot;);</code> → <code>expect(presentation.backgroundColor).toBe(&quot;rgb(20, 42, 35)&quot;);</code> |
| `tests/e2e/public-release.spec.ts:281` | `stale on main (fails at c619ac6)` | <code>const controls = page.locator(&quot;.equipment-control button&quot;);</code> → <code>const controls = page.locator(&quot;.profile-links a&quot;);</code> |
| `tests/e2e/pwa-offline.spec.ts:37` | `stale on main (fails at c619ac6)` | <code>page.getByRole(&quot;heading&quot;, { level: 1, name: &quot;Your workout. Your way.&quot; }),</code> → <code>page.getByRole(&quot;heading&quot;, { level: 1, name: &quot;A little space for your next set.&quot; }),</code> |
| `tests/e2e/pwa-offline.spec.ts:124` | `stale on main (fails at c619ac6)` | <code>await expect(page.locator(&quot;.offline-indicator&quot;)).toContainText(&quot;Offline&quot;);</code> → <code>await expect(page.locator(&quot;.offline-indicator&quot;)).toContainText(&quot;offline&quot;);</code> |
| `tests/e2e/pwa-offline.spec.ts:128` | `stale on main (fails at c619ac6)` | <code>page.getByRole(&quot;heading&quot;, { level: 1, name: &quot;Your workout. Your way.&quot; }),</code> → <code>page.getByRole(&quot;heading&quot;, { level: 1, name: &quot;A little space for your next set.&quot; }),</code> |
| `tests/e2e/pwa-offline.spec.ts:133` | `stale on main (fails at c619ac6)` | <code>.locator(&#x27;[data-companion-placement=&quot;landing&quot;] img&#x27;)</code> → <code>.locator(&#x27;.quiet-studio img&#x27;)</code> |
| `tests/e2e/pwa-offline.spec.ts:154` | `stale on main (fails at c619ac6)` | <code>await expect(page.locator(&quot;.offline-indicator&quot;)).toContainText(&quot;Offline&quot;);</code> → <code>await expect(page.locator(&quot;.offline-indicator&quot;)).toContainText(&quot;offline&quot;);</code> |
| `tests/unit/member-program-home.test.tsx:116` | `plan-changed` | <code>expect(markup).toContain(&quot;1 movements&quot;); ⏎ expect(markup).toContain(&quot;1 movements&quot;);</code> → <code>expect(markup).toContain(&quot;&lt;small&gt;1 movement&lt;/small&gt;&quot;);</code> |
| `tests/unit/member-program-home.test.tsx:175` | `plan-changed` | <code>expect(markup).toContain(&quot;Mobility&quot;);</code> → <code>expect(markup).toContain(&#x27;href=&quot;/app/program/70000000-0000-4000-8000-000000000001&quot;&gt;&lt;span&gt;01&lt;/span&gt;&lt;strong&gt;Mobility&lt;/strong&gt;&#x27;);</code> |

### Complete audit ledger

This ledger compares the current files with `9d5162d`, preserving the complete old → new text for the original audit and this follow-up. It supersedes the earlier report's duplicated plural-count and bare Mobility entries. The four acceptance files are excluded because they have no diff.

| File and final line | Old → new |
| --- | --- |
| `tests/authenticated-e2e/animal-surface-pilot-authenticated.spec.ts:167` | <code>await page.getByRole(&quot;button&quot;, { name: &quot;Start with example&quot; }).click(); ⏎ await expect(page.getByRole(&quot;heading&quot;, { name: &quot;Choose a training day&quot; })).toBeVisible();</code> → <code>await page.getByRole(&quot;radio&quot;, { name: /Example routine/ }).check(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Continue&quot;, exact: true }).click(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Continue&quot;, exact: true }).click(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Save routine&quot;, exact: true }).click(); ⏎ await expect(page.getByRole(&quot;heading&quot;, { name: &quot;All days&quot; })).toBeVisible();</code> |
| `tests/authenticated-e2e/animal-surface-pilot-authenticated.spec.ts:189` | <code>await page.getByRole(&quot;link&quot;, { name: /Open Push to start/u }).click();</code> → <code>await page.getByRole(&quot;link&quot;, { name: /Push/ }).click();</code> |
| `tests/authenticated-e2e/animal-surface-pilot-authenticated.spec.ts:195` | <code>await page.getByRole(&quot;button&quot;, { name: &quot;Start or resume workout&quot; }).click();</code> → <code>await page.getByRole(&quot;button&quot;, { name: &quot;Start workout&quot; }).click();</code> |
| `tests/authenticated-e2e/animal-surface-pilot-authenticated.spec.ts:269` | <code>await page.getByRole(&quot;button&quot;, { name: &quot;Start with example&quot; }).click(); ⏎ await expect(page.getByRole(&quot;heading&quot;, { name: &quot;Choose a training day&quot; })).toBeVisible();</code> → <code>await page.getByRole(&quot;radio&quot;, { name: /Example routine/ }).check(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Continue&quot;, exact: true }).click(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Continue&quot;, exact: true }).click(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Save routine&quot;, exact: true }).click(); ⏎ await expect(page.getByRole(&quot;heading&quot;, { name: &quot;All days&quot; })).toBeVisible();</code> |
| `tests/authenticated-e2e/animal-surface-pilot-authenticated.spec.ts:331` | <code>await page.getByRole(&quot;button&quot;, { name: &quot;Start with example&quot; }).click(); ⏎ await expect(page.getByRole(&quot;heading&quot;, { name: &quot;Choose a training day&quot; })).toBeVisible();</code> → <code>await page.getByRole(&quot;radio&quot;, { name: /Example routine/ }).check(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Continue&quot;, exact: true }).click(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Continue&quot;, exact: true }).click(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Save routine&quot;, exact: true }).click(); ⏎ await expect(page.getByRole(&quot;heading&quot;, { name: &quot;All days&quot; })).toBeVisible();</code> |
| `tests/authenticated-e2e/animal-surface-pilot-authenticated.spec.ts:342` | <code>const loading = page.locator(&#x27;.member-state[role=&quot;status&quot;]&#x27;); ⏎ await expect(loading).toContainText(&quot;Loading your home…&quot;);</code> → <code>const loading = page.locator(&#x27;.member-state[aria-busy=&quot;true&quot;]&#x27;); ⏎ await expect(loading).toContainText(&quot;Loading…&quot;);</code> |
| `tests/authenticated-e2e/animal-surface-pilot-authenticated.spec.ts:347` | <code>await expect(page.getByRole(&quot;heading&quot;, { name: /Welcome back/u })).toBeVisible();</code> → <code>await expect(page.getByRole(&quot;heading&quot;, { name: /Ready when you are/u })).toBeVisible();</code> |
| `tests/authenticated-e2e/animal-surface-pilot-authenticated.spec.ts:356` | <code>await expect(error).toContainText(&quot;Your home did not load.&quot;); ⏎ await expect(error).toContainText(&quot;No routine or workout changes were made.&quot;);</code> → <code>await expect(error).toContainText(&quot;This page didn&#x27;t load&quot;); ⏎ await expect(error).toContainText(&quot;Nothing was changed.&quot;);</code> |
| `tests/authenticated-e2e/animal-surface-pilot-authenticated.spec.ts:361` | <code>await expect(page.getByRole(&quot;heading&quot;, { name: /Welcome back/u })).toBeVisible();</code> → <code>await expect(page.getByRole(&quot;heading&quot;, { name: /Ready when you are/u })).toBeVisible();</code> |
| `tests/authenticated-e2e/animal-surface-rollout-authenticated.spec.ts:211` | <code>await page.getByRole(&quot;button&quot;, { name: &quot;Start with example&quot; }).click(); ⏎ await expect(page.getByRole(&quot;heading&quot;, { name: &quot;Choose a training day&quot; })).toBeVisible();</code> → <code>await page.getByRole(&quot;radio&quot;, { name: /Example routine/ }).check(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Continue&quot;, exact: true }).click(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Continue&quot;, exact: true }).click(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Save routine&quot;, exact: true }).click(); ⏎ await expect(page.getByRole(&quot;heading&quot;, { name: &quot;All days&quot; })).toBeVisible();</code> |
| `tests/authenticated-e2e/animal-surface-rollout-authenticated.spec.ts:225` | <code>await expectCompanion(page, &quot;library&quot;, width &gt;= 1024, [</code> → <code>await expectCompanion(page, &quot;library&quot;, true, [</code> |
| `tests/authenticated-e2e/animal-surface-rollout-authenticated.spec.ts:244` | <code>await expect(page.getByRole(&quot;heading&quot;, { name: &quot;Edit your route&quot; })).toBeVisible();</code> → <code>await expect(page.getByRole(&quot;heading&quot;, { name: &quot;Your routine&quot; })).toBeVisible();</code> |
| `tests/authenticated-e2e/animal-surface-rollout-authenticated.spec.ts:298` | <code>await page.getByLabel(&quot;Program name&quot;).fill(&quot;Draft state hides decoration&quot;);</code> → <code>await page.getByLabel(&quot;Routine name&quot;).fill(&quot;Draft state hides decoration&quot;);</code> |
| `tests/authenticated-e2e/animal-surface-rollout-authenticated.spec.ts:300` | <code>await expect(page.getByText(&quot;Unpublished changes&quot;)).toBeVisible();</code> → <code>await expect(page.getByText(&quot;Unsaved changes&quot;, { exact: true })).toBeVisible();</code> |
| `tests/authenticated-e2e/animal-surface-rollout-authenticated.spec.ts:320` | <code>await expect(page.getByText(&quot;Preferences saved.&quot;)).toBeVisible();</code> → <code>await expect(page.getByText(&quot;Saved.&quot;, { exact: true })).toBeVisible();</code> |
| `tests/authenticated-e2e/animal-surface-rollout-authenticated.spec.ts:324` | <code>await page.getByRole(&quot;link&quot;, { name: /Open Push to start/u }).click();</code> → <code>await page.getByRole(&quot;link&quot;, { name: /Push/ }).click();</code> |
| `tests/authenticated-e2e/animal-surface-rollout-authenticated.spec.ts:330` | <code>await page.getByRole(&quot;button&quot;, { name: &quot;Start or resume workout&quot; }).click();</code> → <code>await page.getByRole(&quot;button&quot;, { name: &quot;Start workout&quot; }).click();</code> |
| `tests/authenticated-e2e/animal-surface-rollout-authenticated.spec.ts:364` | <code>await expect(page.getByRole(&quot;heading&quot;, { name: &quot;Offline queued&quot; })).toBeVisible();</code> → <code>await expect(page.getByRole(&quot;heading&quot;, { name: &quot;You&#x27;re offline&quot; })).toBeVisible();</code> |
| `tests/authenticated-e2e/animal-surface-rollout-authenticated.spec.ts:367` | <code>await expect(page.getByRole(&quot;heading&quot;, { name: &quot;Offline queued&quot; })).toHaveCount(0);</code> → <code>await expect(page.getByRole(&quot;heading&quot;, { name: &quot;You&#x27;re offline&quot; })).toHaveCount(0);</code> |
| `tests/authenticated-e2e/animal-surface-rollout-authenticated.spec.ts:395` | <code>await page.getByLabel(&quot;Duration (seconds)&quot;).last().fill(&quot;1200&quot;);</code> → <code>await page.getByLabel(&quot;Duration&quot;, { exact: true }).fill(&quot;20:00&quot;);</code> |
| `tests/authenticated-e2e/animal-surface-rollout-authenticated.spec.ts:415` | <code>await page.getByRole(&quot;button&quot;, { name: &quot;Skip exercise&quot; }).click();</code> → <code>if (await page.locator(&quot;.runner-more&quot;).getAttribute(&quot;open&quot;) === null) { ⏎ await page.getByText(&quot;More options&quot;, { exact: true }).click(); ⏎ } ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Skip exercise&quot;, exact: true }).click(); ⏎ await page.getByRole(&quot;dialog&quot;).getByRole(&quot;button&quot;, { name: &quot;Skip exercise&quot;, exact: true }).click();</code> |
| `tests/authenticated-e2e/animal-surface-rollout-authenticated.spec.ts:430` | <code>await page.getByRole(&quot;button&quot;, { name: &quot;Complete workout&quot; }).click();</code> → <code>await page.getByRole(&quot;button&quot;, { name: &quot;Finish workout&quot; }).click();</code> |
| `tests/authenticated-e2e/animal-surface-rollout-authenticated.spec.ts:490` | <code>await page.getByRole(&quot;button&quot;, { name: &quot;Start with example&quot; }).click(); ⏎ await expect(page.getByRole(&quot;heading&quot;, { name: &quot;Choose a training day&quot; })).toBeVisible();</code> → <code>await page.getByRole(&quot;radio&quot;, { name: /Example routine/ }).check(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Continue&quot;, exact: true }).click(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Continue&quot;, exact: true }).click(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Save routine&quot;, exact: true }).click(); ⏎ await expect(page.getByRole(&quot;heading&quot;, { name: &quot;All days&quot; })).toBeVisible();</code> |
| `tests/authenticated-e2e/animal-surface-rollout-authenticated.spec.ts:502` | <code>(absent)</code> → <code>await placement.locator(&quot;img&quot;).evaluate((image) =&gt; image.dispatchEvent(new Event(&quot;error&quot;)));</code> |
| `tests/authenticated-e2e/animal-surface-rollout-authenticated.spec.ts:531` | <code>await page.getByRole(&quot;button&quot;, { name: &quot;Start with example&quot; }).click(); ⏎ await expect(page.getByRole(&quot;heading&quot;, { name: &quot;Choose a training day&quot; })).toBeVisible(); ⏎ await page.getByRole(&quot;link&quot;, { name: /Open Push to start/u }).click();</code> → <code>await page.getByRole(&quot;radio&quot;, { name: /Example routine/ }).check(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Continue&quot;, exact: true }).click(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Continue&quot;, exact: true }).click(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Save routine&quot;, exact: true }).click(); ⏎ await expect(page.getByRole(&quot;heading&quot;, { name: &quot;All days&quot; })).toBeVisible(); ⏎ await page.getByRole(&quot;link&quot;, { name: /Push/ }).click();</code> |
| `tests/authenticated-e2e/animal-surface-rollout-authenticated.spec.ts:542` | <code>await page.getByRole(&quot;button&quot;, { name: &quot;Start or resume workout&quot; }).click();</code> → <code>await page.getByRole(&quot;button&quot;, { name: &quot;Start workout&quot; }).click();</code> |
| `tests/authenticated-e2e/animal-surface-rollout-authenticated.spec.ts:550` | <code>await page.getByLabel(&quot;Duration (seconds)&quot;).last().fill(&quot;1200&quot;);</code> → <code>await page.getByLabel(&quot;Duration&quot;, { exact: true }).fill(&quot;20:00&quot;);</code> |
| `tests/authenticated-e2e/animal-surface-rollout-authenticated.spec.ts:570` | <code>await page.getByRole(&quot;button&quot;, { name: &quot;Skip exercise&quot; }).click();</code> → <code>if (await page.locator(&quot;.runner-more&quot;).getAttribute(&quot;open&quot;) === null) { ⏎ await page.getByText(&quot;More options&quot;, { exact: true }).click(); ⏎ } ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Skip exercise&quot;, exact: true }).click(); ⏎ await page.getByRole(&quot;dialog&quot;).getByRole(&quot;button&quot;, { name: &quot;Skip exercise&quot;, exact: true }).click();</code> |
| `tests/authenticated-e2e/animal-surface-rollout-authenticated.spec.ts:584` | <code>await page.getByRole(&quot;button&quot;, { name: &quot;Complete workout&quot; }).click();</code> → <code>await page.getByRole(&quot;button&quot;, { name: &quot;Finish workout&quot; }).click();</code> |
| `tests/authenticated-e2e/customization-geometry.spec.ts:73` | <code>const sizes = await page.locator(&quot;.member-nav a&quot;).evaluateAll((links) =&gt;</code> → <code>const sizes = await page.locator(&quot;.member-nav a, .quiet-settings-link&quot;).evaluateAll((links) =&gt;</code> |
| `tests/authenticated-e2e/customization-geometry.spec.ts:169` | <code>await expect(page.getByRole(&quot;heading&quot;, { name: &quot;Start with the example or start blank&quot; })).toBeVisible(); ⏎ await page.getByLabel(&quot;Time zone&quot;).fill(&quot;America/Chicago&quot;);</code> → <code>await expect(page.getByRole(&quot;heading&quot;, { name: &quot;Make room for your routine.&quot; })).toBeVisible(); ⏎ await page.getByRole(&quot;radio&quot;, { name: /Example routine/ }).check(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Continue&quot;, exact: true }).click(); ⏎ await page.getByLabel(&quot;Time zone&quot;).selectOption(&quot;America/Chicago&quot;); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Continue&quot;, exact: true }).click();</code> |
| `tests/authenticated-e2e/customization-geometry.spec.ts:179` | <code>await page.getByRole(&quot;button&quot;, { name: &quot;Start with example&quot; }).click();</code> → <code>await page.getByRole(&quot;button&quot;, { name: &quot;Save routine&quot;, exact: true }).click();</code> |
| `tests/authenticated-e2e/customization-geometry.spec.ts:181` | <code>await expect(page.getByRole(&quot;heading&quot;, { name: &quot;Choose a training day&quot; })).toBeVisible();</code> → <code>await expect(page.getByRole(&quot;heading&quot;, { name: &quot;All days&quot; })).toBeVisible();</code> |
| `tests/authenticated-e2e/customization-geometry.spec.ts:190` | <code>).toEqual({ dark: true, paper: &quot;#0b252b&quot;, reduced: true, scrollBehavior: &quot;auto&quot; });</code> → <code>).toEqual({ dark: true, paper: &quot;#142a23&quot;, reduced: true, scrollBehavior: &quot;auto&quot; });</code> |
| `tests/authenticated-e2e/customization-geometry.spec.ts:195` | <code>await page.getByRole(&quot;link&quot;, { name: /Manage routines/ }).click(); ⏎ await expect(page.getByRole(&quot;heading&quot;, { name: &quot;Your routes&quot; })).toBeVisible(); ⏎ await expect(page.getByLabel(&quot;Program name&quot;)).toHaveAttribute(&quot;maxlength&quot;, &quot;80&quot;); ⏎ const cloneButton = page.getByRole(&quot;button&quot;, { name: &quot;Clone&quot; }).first();</code> → <code>await page.getByRole(&quot;link&quot;, { name: &quot;Routine&quot;, exact: true }).click(); ⏎ await page.getByRole(&quot;link&quot;, { name: &quot;All routines&quot;, exact: true }).click(); ⏎ await expect(page.getByRole(&quot;heading&quot;, { name: &quot;Your routines&quot; })).toBeVisible(); ⏎ await expect(page.getByLabel(&quot;Routine name&quot;)).toHaveAttribute(&quot;maxlength&quot;, &quot;80&quot;); ⏎ const cloneButton = page.getByRole(&quot;button&quot;, { name: &quot;Duplicate&quot; }).first();</code> |
| `tests/authenticated-e2e/customization-geometry.spec.ts:202` | <code>await expect(page.getByRole(&quot;heading&quot;, { name: /Clone Five-day starter route/ })).toBeVisible(); ⏎ await expect(page.getByLabel(&quot;New program name&quot;)).toHaveAttribute(&quot;maxlength&quot;, &quot;80&quot;);</code> → <code>await expect(page.getByRole(&quot;heading&quot;, { name: /Duplicate Five-day starter route/ })).toBeVisible(); ⏎ await expect(page.getByLabel(&quot;New routine name&quot;)).toHaveAttribute(&quot;maxlength&quot;, &quot;80&quot;);</code> |
| `tests/authenticated-e2e/customization-geometry.spec.ts:210` | <code>await expect(page.getByRole(&quot;heading&quot;, { name: &quot;Edit your route&quot; })).toBeVisible();</code> → <code>await expect(page.getByRole(&quot;heading&quot;, { name: &quot;Your routine&quot; })).toBeVisible(); ⏎ await page.getByText(&quot;Add a section&quot;, { exact: true }).click();</code> |
| `tests/authenticated-e2e/customization-geometry.spec.ts:220` | <code>(absent)</code> → <code>await accessorySection.getByLabel(`More actions for ${accessoryName}`, { exact: true }).click();</code> |
| `tests/authenticated-e2e/customization-geometry.spec.ts:230` | <code>await page.goto(&quot;/app&quot;);</code> → <code>await page.goto(&quot;/app/settings&quot;);</code> |
| `tests/authenticated-e2e/customization-geometry.spec.ts:259` | <code>await expect(page.getByRole(&quot;heading&quot;, { name: &quot;Progress&quot; })).toBeVisible();</code> → <code>await expect(page.getByRole(&quot;heading&quot;, { name: &quot;Progress&quot;, exact: true })).toBeVisible();</code> |
| `tests/authenticated-e2e/firebase-auth-hydration.spec.ts:82` | <code>await page.getByRole(&quot;button&quot;, { name: &quot;Start with example&quot; }).click(); ⏎ await expect(page.getByRole(&quot;heading&quot;, { name: &quot;Choose a training day&quot; })).toBeVisible();</code> → <code>await page.getByRole(&quot;radio&quot;, { name: /Example routine/ }).check(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Continue&quot;, exact: true }).click(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Continue&quot;, exact: true }).click(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Save routine&quot;, exact: true }).click(); ⏎ await expect(page.getByRole(&quot;heading&quot;, { name: &quot;All days&quot; })).toBeVisible();</code> |
| `tests/authenticated-e2e/firebase-auth-hydration.spec.ts:91` | <code>page.getByText(&quot;The browser Firebase sign-in could not be found after initialization.&quot;),</code> → <code>page.getByText(&quot;Please sign in again to delete your account.&quot;),</code> |
| `tests/authenticated-e2e/firebase-auth-hydration.spec.ts:93` | <code>await expect(page.getByRole(&quot;button&quot;, { name: &quot;Review permanent deletion&quot; })).toBeDisabled();</code> → <code>await expect(page.getByRole(&quot;button&quot;, { name: &quot;Delete my account&quot; })).toBeDisabled();</code> |
| `tests/authenticated-e2e/flexible-routine-publication.spec.ts:229` | <code>await page.getByRole(&quot;button&quot;, { name: &quot;Start with example&quot; }).click();</code> → <code>await page.getByRole(&quot;radio&quot;, { name: /Example routine/ }).check(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Continue&quot;, exact: true }).click(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Continue&quot;, exact: true }).click(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Save routine&quot;, exact: true }).click();</code> |
| `tests/authenticated-e2e/flexible-routine-publication.spec.ts:253` | <code>await page.getByRole(&quot;button&quot;, { name }).click();</code> → <code>if (name === &quot;Skip exercise&quot;) { ⏎ if (await page.locator(&quot;.runner-more&quot;).getAttribute(&quot;open&quot;) === null) { ⏎ await page.getByText(&quot;More options&quot;, { exact: true }).click(); ⏎ } ⏎ await page.getByRole(&quot;button&quot;, { name, exact: true }).click(); ⏎ await page.getByRole(&quot;dialog&quot;).getByRole(&quot;button&quot;, { name, exact: true }).click(); ⏎ } else { ⏎ await page.getByRole(&quot;button&quot;, { name, exact: true }).click(); ⏎ }</code> |
| `tests/authenticated-e2e/flexible-routine-publication.spec.ts:302` | <code>await alice.page.getByRole(&quot;link&quot;, { name: /Manage routines/ }).click();</code> → <code>await alice.page.getByRole(&quot;link&quot;, { name: &quot;Routine&quot;, exact: true }).click(); ⏎ await alice.page.getByRole(&quot;link&quot;, { name: &quot;All routines&quot;, exact: true }).click();</code> |
| `tests/authenticated-e2e/flexible-routine-publication.spec.ts:305` | <code>await alice.page.getByLabel(&quot;Program name&quot;).fill(&quot;Weekend route&quot;);</code> → <code>await alice.page.getByLabel(&quot;Routine name&quot;).fill(&quot;Weekend route&quot;);</code> |
| `tests/authenticated-e2e/flexible-routine-publication.spec.ts:314` | <code>await alice.page.getByRole(&quot;button&quot;, { name: &quot;Publish custom routine&quot; }).click();</code> → <code>await alice.page.getByRole(&quot;button&quot;, { name: &quot;Create and use this routine&quot; }).click();</code> |
| `tests/authenticated-e2e/flexible-routine-publication.spec.ts:318` | <code>alice.page.getByText(&quot;Weekend route · Revision 1 · Dumbbells · 1 day&quot;, {</code> → <code>alice.page.getByText(&quot;Weekend route · Dumbbells · 1 day&quot;, {</code> |
| `tests/authenticated-e2e/flexible-routine-publication.spec.ts:323` | <code>await expect(alice.page.getByText(&quot;1 movements · no cardio&quot;)).toBeVisible();</code> → <code>await expect(alice.page.getByText(&quot;1 movement · no cardio&quot;)).toBeVisible();</code> |
| `tests/authenticated-e2e/flexible-routine-publication.spec.ts:340` | <code>await expect(alice.page.getByText(&quot;1 movements · no cardio finish&quot;)).toBeVisible();</code> → <code>await expect(alice.page.getByText(&quot;1 movement · no cardio finish&quot;)).toBeVisible();</code> |
| `tests/authenticated-e2e/flexible-routine-publication.spec.ts:348` | <code>await alice.page.getByRole(&quot;button&quot;, { name: &quot;Start or resume workout&quot; }).click();</code> → <code>await alice.page.getByRole(&quot;button&quot;, { name: &quot;Start workout&quot; }).click();</code> |
| `tests/authenticated-e2e/flexible-routine-publication.spec.ts:354` | <code>const completion = submitRunnerAction(alice.page, &quot;Complete workout&quot;);</code> → <code>const completion = submitRunnerAction(alice.page, &quot;Finish workout&quot;);</code> |
| `tests/authenticated-e2e/flexible-routine-publication.spec.ts:364` | <code>const daySetup = alice.page.getByRole(&quot;region&quot;, { ⏎ name: &quot;Name the day before choosing its first movement&quot;,</code> → <code>const daySetup = alice.page.getByRole(&quot;dialog&quot;, { ⏎ name: &quot;New day&quot;,</code> |
| `tests/authenticated-e2e/flexible-routine-publication.spec.ts:497` | <code>await alice.page.getByRole(&quot;button&quot;, { name: &quot;Publish new revision&quot; }).click();</code> → <code>await alice.page.getByRole(&quot;button&quot;, { name: &quot;Save routine&quot; }).click();</code> |
| `tests/authenticated-e2e/flexible-routine-publication.spec.ts:499` | <code>await expect(alice.page.getByText(/Published revision 2/)).toBeVisible();</code> → <code>await expect(alice.page.locator(&quot;.quiet-save-state&quot;)).toHaveText(&quot;Saved&quot;);</code> |
| `tests/authenticated-e2e/flexible-routine-publication.spec.ts:536` | <code>await expect(alice.page.getByText(&quot;Revision 2 · Dumbbells · 2 days&quot;)).toBeVisible();</code> → <code>await expect(alice.page.getByText(&quot;Dumbbells · 2 days&quot;)).toBeVisible();</code> |
| `tests/authenticated-e2e/flexible-routine-publication.spec.ts:565` | <code>await alice.page.getByRole(&quot;button&quot;, { name: &quot;Start or resume workout&quot; }).click();</code> → <code>await alice.page.getByRole(&quot;button&quot;, { name: &quot;Start workout&quot; }).click();</code> |
| `tests/authenticated-e2e/flexible-routine-publication.spec.ts:591` | <code>await expect(equipmentPage.getByText(/Saved revision 3/)).toBeVisible();</code> → <code>await expect(equipmentPage.getByText(&quot;Equipment updated.&quot;, { exact: true })).toBeVisible();</code> |
| `tests/authenticated-e2e/flexible-routine-publication.spec.ts:625` | <code>await alice.page.getByLabel(&quot;Duration (seconds)&quot;).last().fill(&quot;900&quot;);</code> → <code>await alice.page.getByLabel(&quot;Duration&quot;, { exact: true }).fill(&quot;15:00&quot;);</code> |
| `tests/authenticated-e2e/flexible-routine-publication.spec.ts:628` | <code>const resumedCompletion = submitRunnerAction(alice.page, &quot;Complete workout&quot;);</code> → <code>const resumedCompletion = submitRunnerAction(alice.page, &quot;Finish workout&quot;);</code> |
| `tests/authenticated-e2e/flexible-routine-publication.spec.ts:649` | <code>await expect(bob.page.getByRole(&quot;heading&quot;, { name: &quot;Choose a training day&quot; })).toBeVisible();</code> → <code>await expect(bob.page.getByRole(&quot;heading&quot;, { name: &quot;All days&quot; })).toBeVisible();</code> |
| `tests/authenticated-e2e/library-core-conditioning-expansion.spec.ts:193` | <code>await page.getByRole(&quot;button&quot;, { name: &quot;Start with example&quot; }).click();</code> → <code>await page.getByRole(&quot;radio&quot;, { name: /Example routine/ }).check(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Continue&quot;, exact: true }).click(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Continue&quot;, exact: true }).click(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Save routine&quot;, exact: true }).click();</code> |
| `tests/authenticated-e2e/library-core-conditioning-expansion.spec.ts:209` | <code>`^${escapeRegExp(name)} (?:weight reps&#124;bodyweight reps&#124;duration&#124;distance duration) ·`,</code> → <code>`^${escapeRegExp(name)} (?:Weight and reps&#124;Reps&#124;Time&#124;Distance and time) ·`,</code> |
| `tests/authenticated-e2e/library-core-conditioning-expansion.spec.ts:244` | <code>await expect(alice.page.getByRole(&quot;heading&quot;, { name: &quot;Choose a training day&quot; })).toBeVisible();</code> → <code>await expect(alice.page.getByRole(&quot;heading&quot;, { name: &quot;All days&quot; })).toBeVisible();</code> |
| `tests/authenticated-e2e/library-core-conditioning-expansion.spec.ts:267` | <code>await expect(clean.getByText(&quot;strength · weight reps&quot;, { exact: true })).toBeVisible();</code> → <code>await expect(clean.getByText(&quot;strength · Weight and reps&quot;, { exact: true })).toBeVisible();</code> |
| `tests/authenticated-e2e/library-core-conditioning-expansion.spec.ts:270` | <code>await expect(crunch.getByText(&quot;strength · bodyweight reps&quot;, { exact: true })).toBeVisible();</code> → <code>await expect(crunch.getByText(&quot;strength · Reps&quot;, { exact: true })).toBeVisible();</code> |
| `tests/authenticated-e2e/library-core-conditioning-expansion.spec.ts:273` | <code>await expect(flutter.getByText(&quot;strength · duration&quot;, { exact: true })).toBeVisible();</code> → <code>await expect(flutter.getByText(&quot;strength · Time&quot;, { exact: true })).toBeVisible();</code> |
| `tests/authenticated-e2e/library-core-conditioning-expansion.spec.ts:276` | <code>await expect(carry.getByText(&quot;strength · distance duration&quot;, { exact: true })).toBeVisible();</code> → <code>await expect(carry.getByText(&quot;strength · Distance and time&quot;, { exact: true })).toBeVisible();</code> |
| `tests/authenticated-e2e/library-core-conditioning-expansion.spec.ts:290` | <code>await alice.page.getByRole(&quot;button&quot;, { name: &quot;Publish new revision&quot; }).click();</code> → <code>await alice.page.getByRole(&quot;button&quot;, { name: &quot;Save routine&quot; }).click();</code> |
| `tests/authenticated-e2e/library-core-conditioning-expansion.spec.ts:318` | <code>await alice.page.getByRole(&quot;button&quot;, { name: &quot;Publish new revision&quot; }).click();</code> → <code>await alice.page.getByRole(&quot;button&quot;, { name: &quot;Save routine&quot; }).click();</code> |
| `tests/authenticated-e2e/library-core-conditioning-expansion.spec.ts:321` | <code>await expect(alice.page.getByText(/Published revision 2/u)).toBeVisible();</code> → <code>await expect(alice.page.locator(&quot;.quiet-save-state&quot;)).toHaveText(&quot;Saved&quot;);</code> |
| `tests/authenticated-e2e/library-core-conditioning-expansion.spec.ts:361` | <code>await alice.page.getByRole(&quot;button&quot;, { name: &quot;Start or resume workout&quot; }).click();</code> → <code>await alice.page.getByRole(&quot;button&quot;, { name: &quot;Start workout&quot; }).click();</code> |
| `tests/authenticated-e2e/library-core-conditioning-expansion.spec.ts:366` | <code>(absent)</code> → <code>await alice.page.getByText(&quot;Workout outline&quot;, { exact: true }).click();</code> |
| `tests/authenticated-e2e/library-core-conditioning-expansion.spec.ts:369` | <code>(absent)</code> → <code>await alice.page.getByText(&quot;Watch demo and technique guidance&quot;, { exact: true }).click();</code> |
| `tests/authenticated-e2e/library-core-conditioning-expansion.spec.ts:374` | <code>&quot;No approved catalog pair is available for this movement. Workout logging remains available.&quot;,</code> → <code>&quot;No demonstration is available for this movement. Workout logging remains available.&quot;,</code> |
| `tests/authenticated-e2e/library-core-conditioning-expansion.spec.ts:387` | <code>await alice.page.getByRole(&quot;button&quot;, { name: &quot;Save set&quot; }).click();</code> → <code>await alice.page.getByRole(&quot;button&quot;, { name: /^(Log&#124;Update) set &amp; rest$/ }).click();</code> |
| `tests/authenticated-e2e/library-guidance.spec.ts:87` | <code>await page.getByRole(&quot;button&quot;, { name: &quot;Start with example&quot; }).click();</code> → <code>await page.getByRole(&quot;radio&quot;, { name: /Example routine/ }).check(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Continue&quot;, exact: true }).click(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Continue&quot;, exact: true }).click(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Save routine&quot;, exact: true }).click();</code> |
| `tests/authenticated-e2e/library-guidance.spec.ts:92` | <code>await expect(page.getByRole(&quot;heading&quot;, { name: &quot;Choose a training day&quot; })).toBeVisible();</code> → <code>await expect(page.getByRole(&quot;heading&quot;, { name: &quot;All days&quot; })).toBeVisible();</code> |
| `tests/authenticated-e2e/library-guidance.spec.ts:125` | <code>await reopened.getByRole(&quot;button&quot;, { name: /Suitcase march/ }).click();</code> → <code>await reopened.locator(&quot;.movement-chooser__list&quot;).getByRole(&quot;button&quot;, { name: /Suitcase march/ }).click();</code> |
| `tests/authenticated-e2e/library-guidance.spec.ts:217` | <code>await page.getByRole(&quot;button&quot;, { name: &quot;Start or resume workout&quot; }).click();</code> → <code>await page.getByRole(&quot;button&quot;, { name: &quot;Start workout&quot; }).click();</code> |
| `tests/authenticated-e2e/library-guidance.spec.ts:221` | <code>(absent)</code> → <code>await page.getByText(&quot;Watch demo and technique guidance&quot;, { exact: true }).click();</code> |
| `tests/authenticated-e2e/library-guidance.spec.ts:245` | <code>(absent)</code> → <code>await page.getByText(&quot;Watch demo and technique guidance&quot;, { exact: true }).click();</code> |
| `tests/authenticated-e2e/library-strength-expansion.spec.ts:90` | <code>await page.getByRole(&quot;button&quot;, { name: &quot;Start with example&quot; }).click();</code> → <code>await page.getByRole(&quot;radio&quot;, { name: /Example routine/ }).check(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Continue&quot;, exact: true }).click(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Continue&quot;, exact: true }).click(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Save routine&quot;, exact: true }).click();</code> |
| `tests/authenticated-e2e/library-strength-expansion.spec.ts:95` | <code>await expect(page.getByRole(&quot;heading&quot;, { name: &quot;Choose a training day&quot; })).toBeVisible();</code> → <code>await expect(page.getByRole(&quot;heading&quot;, { name: &quot;All days&quot; })).toBeVisible();</code> |
| `tests/authenticated-e2e/library-strength-expansion.spec.ts:136` | <code>await page.getByRole(&quot;button&quot;, { name: &quot;Publish new revision&quot; }).click();</code> → <code>await page.getByRole(&quot;button&quot;, { name: &quot;Save routine&quot; }).click();</code> |
| `tests/authenticated-e2e/library-strength-expansion.spec.ts:138` | <code>await expect(page.getByText(/Published revision \d+/u)).toBeVisible();</code> → <code>await expect(page.locator(&quot;.quiet-save-state&quot;)).toHaveText(&quot;Saved&quot;);</code> |
| `tests/authenticated-e2e/library-strength-expansion.spec.ts:165` | <code>await page.getByRole(&quot;button&quot;, { name: &quot;Start or resume workout&quot; }).click();</code> → <code>await page.getByRole(&quot;button&quot;, { name: &quot;Start workout&quot; }).click();</code> |
| `tests/authenticated-e2e/library-strength-expansion.spec.ts:169` | <code>(absent)</code> → <code>await page.getByText(&quot;Workout outline&quot;, { exact: true }).click();</code> |
| `tests/authenticated-e2e/library-strength-expansion.spec.ts:174` | <code>const techniquePanel = page.locator(&quot;section.runner-technique&quot;);</code> → <code>await page.getByText(&quot;Watch demo and technique guidance&quot;, { exact: true }).click(); ⏎ const techniquePanel = page.locator(&quot;details.runner-technique&quot;);</code> |
| `tests/authenticated-e2e/library-strength-expansion.spec.ts:179` | <code>&quot;No approved catalog pair is available for this movement. Workout logging remains available.&quot;,</code> → <code>&quot;No demonstration is available for this movement. Workout logging remains available.&quot;,</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:256` | <code>(absent)</code> → <code>if (await page.getByRole(&quot;radio&quot;, { name: /Example routine/ }).isVisible()) { ⏎ await page.getByRole(&quot;radio&quot;, { name: mode === &quot;example&quot; ? /Example routine/ : /Blank routine/ }).check(); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Continue&quot;, exact: true }).click(); ⏎ } ⏎ if (await page.getByLabel(&quot;Display units&quot;).isVisible()) { ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Continue&quot;, exact: true }).click(); ⏎ }</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:266` | <code>if (mode === &quot;blank&quot;) { ⏎ await page.getByRole(&quot;radio&quot;, { name: /Blank routine/ }).check();</code> → <code>if (mode === &quot;blank&quot; &amp;&amp; await page.getByLabel(&quot;Search movements&quot;).isVisible()) { ⏎ await page.getByLabel(&quot;Search movements&quot;).fill(&quot;Dead bug&quot;); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;Dead bug bodyweight&quot;, exact: true }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:276` | <code>name: mode === &quot;example&quot; ? &quot;Start with example&quot; : &quot;Start blank&quot;,</code> → <code>name: &quot;Save routine&quot;, exact: true,</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:289` | <code>await chooser.getByRole(&quot;button&quot;, { name }).click();</code> → <code>await chooser.locator(&quot;.movement-chooser__list&quot;).getByRole(&quot;button&quot;, { name }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:393` | <code>const saveButton = page.getByRole(&quot;button&quot;, { name: &quot;Save set&quot; });</code> → <code>const saveButton = page.getByRole(&quot;button&quot;, { name: /^(Log&#124;Update) set &amp; rest$/ });</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:410` | <code>await page.getByRole(&quot;button&quot;, { name }).click();</code> → <code>if (name === &quot;Skip exercise&quot;) { ⏎ if (await page.locator(&quot;.runner-more&quot;).getAttribute(&quot;open&quot;) === null) { ⏎ await page.getByText(&quot;More options&quot;, { exact: true }).click(); ⏎ } ⏎ await page.getByRole(&quot;button&quot;, { name, exact: true }).click(); ⏎ await page.getByRole(&quot;dialog&quot;).getByRole(&quot;button&quot;, { name, exact: true }).click(); ⏎ } else { ⏎ await page.getByRole(&quot;button&quot;, { name, exact: true }).click(); ⏎ }</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:434` | <code>await page.getByRole(&quot;button&quot;, { name: &quot;Start or resume workout&quot; }).click();</code> → <code>await page.getByRole(&quot;button&quot;, { name: &quot;Start workout&quot; }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:448` | <code>expect((await submitRunnerAction(page, &quot;Complete exercise&quot;)).status()).toBe(200);</code> → <code>expect((await submitRunnerAction(page, &quot;Next exercise&quot;)).status()).toBe(200); ⏎ await page.getByText(&quot;Workout outline&quot;, { exact: true }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:463` | <code>await page.getByLabel(&quot;Duration (seconds)&quot;).last().fill(&quot;1200&quot;);</code> → <code>await page.getByLabel(&quot;Duration&quot;, { exact: true }).fill(&quot;20:00&quot;);</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:468` | <code>const completionPromise = submitRunnerAction(page, &quot;Complete workout&quot;);</code> → <code>const completionPromise = submitRunnerAction(page, &quot;Finish workout&quot;);</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:499` | <code>await expect(blank.page.getByRole(&quot;heading&quot;, { name: &quot;Edit your route&quot; })).toBeVisible(); ⏎ await expect(blank.page.getByLabel(&quot;Program name&quot;)).toHaveValue(&quot;Blank routine&quot;);</code> → <code>await expect(blank.page.getByRole(&quot;heading&quot;, { name: &quot;Your routine&quot; })).toBeVisible(); ⏎ await expect(blank.page.getByLabel(&quot;Routine name&quot;)).toHaveValue(&quot;Blank routine&quot;);</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:550` | <code>await expect(unverified.page.getByText(&quot;Read-only account.&quot;)).toBeVisible(); ⏎ await expect(unverified.page.getByRole(&quot;button&quot;, { name: &quot;Start with example&quot; })).toBeDisabled();</code> → <code>await expect(unverified.page.getByText(&quot;Verify your email to save changes.&quot;)).toBeVisible(); ⏎ await expect(unverified.page.getByRole(&quot;button&quot;, { name: &quot;Continue&quot;, exact: true })).toBeDisabled();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:568` | <code>await expect(alice.page.getByRole(&quot;heading&quot;, { name: &quot;Choose a training day&quot; })).toBeVisible(); ⏎ await expect(alice.page.getByRole(&quot;heading&quot;, { name: &quot;Welcome back, Alice QA&quot; })).toBeVisible();</code> → <code>await expect(alice.page.getByRole(&quot;heading&quot;, { name: &quot;All days&quot; })).toBeVisible(); ⏎ await expect(alice.page.getByRole(&quot;heading&quot;, { name: &quot;Ready when you are, Alice QA.&quot; })).toBeVisible();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:579` | <code>await expect(alice.page.getByText(&quot;Revision 1 · Dumbbells · 5 days&quot;)).toBeVisible();</code> → <code>await expect(alice.page.getByText(&quot;Dumbbells · 5 days&quot;)).toBeVisible();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:584` | <code>await alice.page.getByRole(&quot;link&quot;, { name: /Open Push to start/ }).click();</code> → <code>await alice.page.getByRole(&quot;link&quot;, { name: /Push/ }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:590` | <code>await alice.page.getByRole(&quot;button&quot;, { name: &quot;Start or resume workout&quot; }).click();</code> → <code>await alice.page.getByRole(&quot;button&quot;, { name: &quot;Start workout&quot; }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:643` | <code>await expect(bob.page.getByText(&quot;Revision 1 · Barbell + rack · 5 days&quot;)).toBeVisible();</code> → <code>await expect(bob.page.getByText(&quot;Barbell + rack · 5 days&quot;)).toBeVisible();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:674` | <code>await expect(bob.page.getByRole(&quot;heading&quot;, { name: &quot;Choose a training day&quot; })).toBeVisible();</code> → <code>await expect(bob.page.getByRole(&quot;heading&quot;, { name: &quot;All days&quot; })).toBeVisible();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:727` | <code>await expect(page.getByRole(&quot;heading&quot;, { name: &quot;Start with the example or start blank&quot; })).toBeVisible();</code> → <code>await expect(page.getByRole(&quot;heading&quot;, { name: &quot;Make room for your routine.&quot; })).toBeVisible();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:734` | <code>await expect(page.getByRole(&quot;heading&quot;, { name: &quot;Choose a training day&quot; })).toBeVisible();</code> → <code>await expect(page.getByRole(&quot;heading&quot;, { name: &quot;All days&quot; })).toBeVisible();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:749` | <code>await alice.page.getByRole(&quot;link&quot;, { name: /Edit routine/ }).click();</code> → <code>await alice.page.getByRole(&quot;link&quot;, { name: &quot;Routine&quot;, exact: true }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:755` | <code>await expect(alice.page.getByText(&quot;Unpublished changes&quot;)).toBeVisible();</code> → <code>await expect(alice.page.getByText(&quot;Unsaved changes&quot;, { exact: true })).toBeVisible();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:757` | <code>(absent)</code> → <code>await alice.page.getByText(&quot;Equipment and substitutions&quot;, { exact: true }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:769` | <code>alice.page.getByText(/Your unpublished editor changes are not included/),</code> → <code>alice.page.getByText(&quot;Save or discard your routine edits first.&quot;),</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:780` | <code>expect(dialog.message()).toBe(&quot;Discard this unpublished program draft?&quot;);</code> → <code>expect(dialog.message()).toBe(&quot;Discard your routine changes?&quot;);</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:791` | <code>await expect(alice.page.getByText(&quot;Unpublished changes&quot;)).toBeVisible();</code> → <code>await expect(alice.page.getByText(&quot;Unsaved changes&quot;, { exact: true })).toBeVisible();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:800` | <code>await expect(alice.page.getByRole(&quot;heading&quot;, { name: &quot;Choose a training day&quot; })).toBeVisible();</code> → <code>await expect(alice.page.getByRole(&quot;heading&quot;, { name: &quot;All days&quot; })).toBeVisible();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:823` | <code>await alice.page.getByLabel(&quot;Time zone&quot;).fill(&quot;UTC&quot;);</code> → <code>await alice.page.getByRole(&quot;radio&quot;, { name: /Example routine/ }).check(); ⏎ await alice.page.getByRole(&quot;button&quot;, { name: &quot;Continue&quot;, exact: true }).click(); ⏎ await alice.page.getByLabel(&quot;Time zone&quot;).selectOption(&quot;UTC&quot;);</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:830` | <code>await alice.page.getByRole(&quot;link&quot;, { name: &quot;Home&quot;, exact: true }).click(); ⏎ await alice.page.getByRole(&quot;link&quot;, { name: /Manage routines/ }).click(); ⏎ await alice.page.getByLabel(&quot;Program name&quot;).fill(&quot;QA barbell route&quot;);</code> → <code>await alice.page.getByRole(&quot;link&quot;, { name: &quot;Today&quot;, exact: true }).click(); ⏎ await alice.page.getByRole(&quot;link&quot;, { name: &quot;Routine&quot;, exact: true }).click(); ⏎ await alice.page.getByRole(&quot;link&quot;, { name: &quot;All routines&quot;, exact: true }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:834` | <code>(absent)</code> → <code>await alice.page.getByLabel(&quot;Routine name&quot;).fill(&quot;QA barbell route&quot;);</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:840` | <code>await alice.page.getByRole(&quot;button&quot;, { name: &quot;Create from example&quot; }).click();</code> → <code>await alice.page.getByRole(&quot;button&quot;, { name: &quot;Create and use this routine&quot; }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:842` | <code>await expect(alice.page.getByText(&quot;Revision 1 · Barbell + rack · 5 days&quot;)).toBeVisible();</code> → <code>await expect(alice.page.getByText(&quot;Barbell + rack · 5 days&quot;)).toBeVisible();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:845` | <code>await alice.page.getByRole(&quot;link&quot;, { name: /Manage routines/ }).click();</code> → <code>await alice.page.getByRole(&quot;link&quot;, { name: &quot;Routine&quot;, exact: true }).click(); ⏎ await alice.page.getByRole(&quot;link&quot;, { name: &quot;All routines&quot;, exact: true }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:850` | <code>await barbellProgramCard.getByRole(&quot;button&quot;, { name: &quot;Clone&quot; }).click(); ⏎ await alice.page.getByLabel(&quot;New program name&quot;).fill(&quot;QA cloned route&quot;);</code> → <code>await barbellProgramCard.getByRole(&quot;button&quot;, { name: &quot;Duplicate&quot; }).click(); ⏎ await alice.page.getByLabel(&quot;New routine name&quot;).fill(&quot;QA cloned route&quot;);</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:857` | <code>await alice.page.getByRole(&quot;button&quot;, { name: &quot;Clone and activate&quot; }).click();</code> → <code>await alice.page.getByRole(&quot;dialog&quot;).getByRole(&quot;button&quot;, { name: &quot;Duplicate&quot;, exact: true }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:859` | <code>(absent)</code> → <code>await alice.page.getByRole(&quot;link&quot;, { name: &quot;Back to Today&quot;, exact: true }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:861` | <code>alice.page.getByText(&quot;QA cloned route · Revision 1 · Barbell + rack · 5 days&quot;, {</code> → <code>alice.page.getByText(&quot;QA cloned route · Barbell + rack · 5 days&quot;, {</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:873` | <code>await alice.page.getByRole(&quot;link&quot;, { name: /Manage routines/ }).click();</code> → <code>await alice.page.getByRole(&quot;link&quot;, { name: &quot;Routine&quot;, exact: true }).click(); ⏎ await alice.page.getByRole(&quot;link&quot;, { name: &quot;All routines&quot;, exact: true }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:888` | <code>await expect(alice.page.getByText(&quot;Revision 1 · Dumbbells · 5 days&quot;)).toBeVisible();</code> → <code>await expect(alice.page.getByText(&quot;Dumbbells · 5 days&quot;)).toBeVisible();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:890` | <code>await alice.page.getByRole(&quot;link&quot;, { name: /Manage private exercises/ }).click(); ⏎ await alice.page.getByRole(&quot;link&quot;, { name: &quot;Create exercise&quot; }).click();</code> → <code>await alice.page.getByRole(&quot;link&quot;, { name: &quot;Library&quot;, exact: true }).click(); ⏎ await alice.page.getByRole(&quot;link&quot;, { name: &quot;Create private exercise&quot; }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:913` | <code>await alice.page.getByRole(&quot;link&quot;, { name: &quot;Home&quot;, exact: true }).click(); ⏎ await alice.page.getByRole(&quot;link&quot;, { name: /Edit routine/ }).click();</code> → <code>await alice.page.getByRole(&quot;link&quot;, { name: &quot;Today&quot;, exact: true }).click(); ⏎ await alice.page.getByRole(&quot;link&quot;, { name: &quot;Routine&quot;, exact: true }).click(); ⏎ await alice.page.getByText(&quot;Add a section&quot;, { exact: true }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:924` | <code>(absent)</code> → <code>await accessorySection.getByLabel(`More actions for ${accessoryName}`, { exact: true }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:964` | <code>(absent)</code> → <code>await customPrescription.getByLabel(&quot;More actions for QA supported row&quot;, { exact: true }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:972` | <code>(absent)</code> → <code>await newAccessorySection.getByLabel(&quot;More actions for Custom assistance&quot;, { exact: true }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:978` | <code>await walkerEditor.getByLabel(&quot;Duration seconds&quot;).fill(&quot;900&quot;);</code> → <code>await walkerEditor.getByLabel(&quot;Duration&quot;, { exact: true }).fill(&quot;15:00&quot;);</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:1005` | <code>await alice.page.getByRole(&quot;button&quot;, { name: &quot;Publish new revision&quot; }).click();</code> → <code>await alice.page.getByRole(&quot;button&quot;, { name: &quot;Save routine&quot; }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:1012` | <code>alice.page.getByText(&quot;The synthetic transport lost the accepted publication response.&quot;),</code> → <code>alice.page.getByText(&quot;Couldn&#x27;t save your routine. Try again.&quot;),</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:1025` | <code>await alice.page.getByRole(&quot;button&quot;, { name: &quot;Publish new revision&quot; }).click();</code> → <code>await alice.page.getByRole(&quot;button&quot;, { name: &quot;Save routine&quot; }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:1030` | <code>await expect(alice.page.getByText(/Published revision 2/)).toBeVisible(); ⏎ await expect(alice.page.getByText(&quot;Draft matches the active revision&quot;)).toBeVisible();</code> → <code>await expect(alice.page.getByText(&quot;Routine saved. Past workouts stay as they were.&quot;, { exact: true })).toBeVisible(); ⏎ await expect(alice.page.locator(&quot;.quiet-save-state&quot;)).toHaveText(&quot;Saved&quot;);</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:1046` | <code>await alice.page.getByRole(&quot;link&quot;, { name: &quot;Back to program&quot; }).click(); ⏎ await expect(alice.page.getByRole(&quot;heading&quot;, { name: &quot;Welcome back, Alice QA&quot; })).toBeVisible();</code> → <code>await alice.page.getByRole(&quot;link&quot;, { name: &quot;Back to Today&quot; }).click(); ⏎ await expect(alice.page.getByRole(&quot;heading&quot;, { name: &quot;Ready when you are, Alice QA.&quot; })).toBeVisible();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:1051` | <code>await alice.page.getByRole(&quot;link&quot;, { name: &quot;Home&quot;, exact: true }).click(); ⏎ await expect(alice.page.getByRole(&quot;heading&quot;, { name: &quot;Welcome back, Alice QA&quot; })).toBeVisible();</code> → <code>await alice.page.getByRole(&quot;link&quot;, { name: &quot;Today&quot;, exact: true }).click(); ⏎ await expect(alice.page.getByRole(&quot;heading&quot;, { name: &quot;Ready when you are, Alice QA.&quot; })).toBeVisible();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:1062` | <code>await alice.page.getByRole(&quot;link&quot;, { name: /Edit routine/ }).click();</code> → <code>await alice.page.getByRole(&quot;link&quot;, { name: &quot;Routine&quot;, exact: true }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:1064` | <code>await expect(alice.page.getByRole(&quot;heading&quot;, { name: &quot;Edit your route&quot; })).toBeVisible();</code> → <code>await expect(alice.page.getByRole(&quot;heading&quot;, { name: &quot;Your routine&quot; })).toBeVisible();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:1068` | <code>(absent)</code> → <code>await alice.page.getByText(&quot;Equipment and substitutions&quot;, { exact: true }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:1096` | <code>&quot;Sets, range, rest, position, and notes stay. Movement-specific targets clear.&quot;,</code> → <code>&quot;Sets, reps, rest and notes carry over.&quot;,</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:1117` | <code>await expect(alice.page.getByText(/Saved revision 3/)).toBeVisible();</code> → <code>await expect(alice.page.getByText(&quot;Equipment updated.&quot;, { exact: true })).toBeVisible();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:1156` | <code>await alice.page.getByLabel(&quot;IANA time zone&quot;).fill(&quot;America/Chicago&quot;);</code> → <code>await alice.page.getByLabel(&quot;Time zone&quot;).selectOption(&quot;America/Chicago&quot;);</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:1173` | <code>await alice.page.getByRole(&quot;link&quot;, { name: &quot;Home&quot;, exact: true }).click(); ⏎ await alice.page.getByRole(&quot;link&quot;, { name: /Edit routine/ }).click();</code> → <code>await alice.page.getByRole(&quot;link&quot;, { name: &quot;Today&quot;, exact: true }).click(); ⏎ await alice.page.getByRole(&quot;link&quot;, { name: &quot;Routine&quot;, exact: true }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:1190` | <code>await expect(alice.page.getByText(&quot;Draft matches the active revision&quot;)).toBeVisible(); ⏎ await alice.page.getByRole(&quot;link&quot;, { name: &quot;Back to program&quot; }).click();</code> → <code>await expect(alice.page.locator(&quot;.quiet-save-state&quot;)).toHaveText(&quot;Saved&quot;); ⏎ await alice.page.getByRole(&quot;link&quot;, { name: &quot;Back to Today&quot; }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:1193` | <code>await alice.page.getByRole(&quot;link&quot;, { name: &quot;History&quot;, exact: true }).click();</code> → <code>await alice.page.getByRole(&quot;link&quot;, { name: &quot;Review history&quot;, exact: true }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:1219` | <code>await expect(alice.page.getByRole(&quot;heading&quot;, { name: &quot;Progress&quot; })).toBeVisible();</code> → <code>await expect(alice.page.getByRole(&quot;heading&quot;, { name: &quot;Progress&quot;, exact: true })).toBeVisible();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:1313` | <code>await alice.page.getByRole(&quot;link&quot;, { name: /Manage private exercises/ }).click(); ⏎ await alice.page.getByRole(&quot;link&quot;, { name: &quot;Create exercise&quot; }).click();</code> → <code>await alice.page.getByRole(&quot;link&quot;, { name: &quot;Library&quot;, exact: true }).click(); ⏎ await alice.page.getByRole(&quot;link&quot;, { name: &quot;Create private exercise&quot; }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:1331` | <code>await alice.page.getByRole(&quot;link&quot;, { name: &quot;Home&quot;, exact: true }).click(); ⏎ await alice.page.getByRole(&quot;link&quot;, { name: /Edit routine/ }).click();</code> → <code>await alice.page.getByRole(&quot;link&quot;, { name: &quot;Today&quot;, exact: true }).click(); ⏎ await alice.page.getByRole(&quot;link&quot;, { name: &quot;Routine&quot;, exact: true }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:1343` | <code>await alice.page.getByRole(&quot;button&quot;, { name: &quot;Publish new revision&quot; }).click();</code> → <code>await alice.page.getByRole(&quot;button&quot;, { name: &quot;Save routine&quot; }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:1345` | <code>await expect(alice.page.getByText(/Published revision 2/)).toBeVisible();</code> → <code>await expect(alice.page.locator(&quot;.quiet-save-state&quot;)).toHaveText(&quot;Saved&quot;);</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:1349` | <code>(absent)</code> → <code>await alice.page.getByText(&quot;Equipment and substitutions&quot;, { exact: true }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:1400` | <code>await alice.page.getByRole(&quot;button&quot;, { name: &quot;Start or resume workout&quot; }).click();</code> → <code>await alice.page.getByRole(&quot;button&quot;, { name: &quot;Start workout&quot; }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:1416` | <code>await expect(alice.page.getByRole(&quot;heading&quot;, { name: &quot;Save activity&quot; })).toBeVisible();</code> → <code>await expect(alice.page.getByRole(&quot;heading&quot;, { name: &quot;Couldn&#x27;t save&quot; })).toBeVisible();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:1449` | <code>await expect(alice.page.getByRole(&quot;heading&quot;, { name: &quot;Save activity&quot; })).toHaveCount(0);</code> → <code>await expect(alice.page.getByRole(&quot;heading&quot;, { name: &quot;Couldn&#x27;t save&quot; })).toHaveCount(0);</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:1456` | <code>expect((await submitRunnerAction(alice.page, &quot;Complete exercise&quot;)).status()).toBe(200);</code> → <code>expect((await submitRunnerAction(alice.page, &quot;Next exercise&quot;)).status()).toBe(200); ⏎ await alice.page.getByText(&quot;Workout outline&quot;, { exact: true }).click();</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:1473` | <code>await alice.page.getByLabel(&quot;Duration (seconds)&quot;).last().fill(&quot;1200&quot;);</code> → <code>await alice.page.getByLabel(&quot;Duration&quot;, { exact: true }).fill(&quot;20:00&quot;);</code> |
| `tests/authenticated-e2e/onboarding.spec.ts:1479` | <code>const completionPromise = submitRunnerAction(alice.page, &quot;Complete workout&quot;);</code> → <code>const completionPromise = submitRunnerAction(alice.page, &quot;Finish workout&quot;);</code> |
| `tests/authenticated-e2e/quiet-set.spec.ts:50` | <code>(absent)</code> → <code>await page.getByLabel(&quot;More actions for Push-up&quot;, { exact: true }).click();</code> |
| `tests/authenticated-e2e/quiet-set.spec.ts:72` | <code>(absent)</code> → <code>await page.getByRole(&quot;link&quot;, {name:&quot;Settings&quot;, exact:true}).click();</code> |
| `tests/authenticated-e2e/quiet-set.spec.ts:74` | <code>(absent)</code> → <code>await page.getByRole(&quot;link&quot;, {name:&quot;Today&quot;, exact:true}).click();</code> |
| `tests/authenticated-e2e/quiet-set.spec.ts:77` | <code>(absent)</code> → <code>await page.getByRole(&quot;link&quot;, {name:&quot;Settings&quot;, exact:true}).click();</code> |
| `tests/authenticated-e2e/quiet-set.spec.ts:79` | <code>(absent)</code> → <code>await page.getByRole(&quot;link&quot;, {name:&quot;Today&quot;, exact:true}).click();</code> |
| `tests/authenticated-e2e/quiet-set.spec.ts:90` | <code>await expect(page.getByRole(&quot;button&quot;, {name:&quot;Complete exercise&quot;, exact:true}).first()).toBeEnabled();</code> → <code>await expect(page.getByRole(&quot;button&quot;, {name:&quot;Finish exercise&quot;, exact:true}).first()).toBeEnabled();</code> |
| `tests/authenticated-e2e/quiet-set.spec.ts:96` | <code>await page.getByRole(&quot;button&quot;, {name:&quot;Complete exercise&quot;, exact:true}).first().click(); ⏎ await page.getByRole(&quot;button&quot;, {name:&quot;Complete workout&quot;, exact:true}).click();</code> → <code>await page.getByRole(&quot;button&quot;, {name:&quot;Finish exercise&quot;, exact:true}).first().click(); ⏎ await page.getByRole(&quot;button&quot;, {name:&quot;Finish workout&quot;, exact:true}).click();</code> |
| `tests/authenticated-e2e/quiet-set.spec.ts:163` | <code>await page.getByRole(&quot;button&quot;,{name:&quot;Complete exercise&quot;,exact:true}).first().click(); ⏎ await page.getByRole(&quot;button&quot;,{name:&quot;Complete workout&quot;,exact:true}).click();</code> → <code>await page.getByRole(&quot;button&quot;,{name:&quot;Finish exercise&quot;,exact:true}).first().click(); ⏎ await page.getByRole(&quot;button&quot;,{name:&quot;Finish workout&quot;,exact:true}).click();</code> |
| `tests/authenticated-e2e/runner-resilience.spec.ts:458` | <code>await page.getByRole(&quot;button&quot;, { name }).click();</code> → <code>if (name === &quot;Skip exercise&quot;) { ⏎ if (await page.locator(&quot;.runner-more&quot;).getAttribute(&quot;open&quot;) === null) { ⏎ await page.getByText(&quot;More options&quot;, { exact: true }).click(); ⏎ } ⏎ await page.getByRole(&quot;button&quot;, { name, exact: true }).click(); ⏎ await page.getByRole(&quot;dialog&quot;).getByRole(&quot;button&quot;, { name, exact: true }).click(); ⏎ } else { ⏎ await page.getByRole(&quot;button&quot;, { name, exact: true }).click(); ⏎ }</code> |
| `tests/authenticated-e2e/runner-resilience.spec.ts:475` | <code>await submitRunnerAction(page, &quot;Complete exercise&quot;);</code> → <code>await submitRunnerAction(page, &quot;Next exercise&quot;);</code> |
| `tests/authenticated-e2e/runner-resilience.spec.ts:492` | <code>await page.getByLabel(&quot;Duration (seconds)&quot;).last().fill(&quot;1200&quot;);</code> → <code>await page.getByLabel(&quot;Duration&quot;, { exact: true }).fill(&quot;20:00&quot;);</code> |
| `tests/authenticated-e2e/runner-resilience.spec.ts:553` | <code>harness.page.getByRole(&quot;heading&quot;, { name: &quot;Offline queued&quot; }),</code> → <code>harness.page.getByRole(&quot;heading&quot;, { name: &quot;You&#x27;re offline&quot; }),</code> |
| `tests/authenticated-e2e/runner-resilience.spec.ts:582` | <code>harness.page.getByRole(&quot;heading&quot;, { name: &quot;Offline queued&quot; }),</code> → <code>harness.page.getByRole(&quot;heading&quot;, { name: &quot;You&#x27;re offline&quot; }),</code> |
| `tests/authenticated-e2e/runner-resilience.spec.ts:690` | <code>name: &quot;Reauthenticate and return&quot;,</code> → <code>name: &quot;Sign in again&quot;,</code> |
| `tests/authenticated-e2e/runner-resilience.spec.ts:807` | <code>first.getByRole(&quot;heading&quot;, { name: &quot;Offline queued&quot; }),</code> → <code>first.getByRole(&quot;heading&quot;, { name: &quot;You&#x27;re offline&quot; }),</code> |
| `tests/authenticated-e2e/runner-resilience.spec.ts:815` | <code>first.getByRole(&quot;heading&quot;, { name: &quot;Offline queued&quot; }),</code> → <code>first.getByRole(&quot;heading&quot;, { name: &quot;You&#x27;re offline&quot; }),</code> |
| `tests/authenticated-e2e/runner-resilience.spec.ts:884` | <code>name: &quot;Choose the workout value to keep&quot;,</code> → <code>name: &quot;Pick which value to keep&quot;,</code> |
| `tests/authenticated-e2e/runner-resilience.spec.ts:890` | <code>name: /Keep 25 lb · 12 reps for Set 1 · Dumbbell bench press/u,</code> → <code>name: /Keep 25 lb · 12 reps/u,</code> |
| `tests/authenticated-e2e/runner-resilience.spec.ts:893` | <code>name: /Keep 30 lb · 12 reps for Set 1 · Dumbbell bench press/u,</code> → <code>name: /Keep 30 lb · 12 reps/u,</code> |
| `tests/authenticated-e2e/runner-resilience.spec.ts:929` | <code>await first ⏎ .getByRole(&quot;button&quot;, { name: &quot;Leave both values unresolved&quot; }) ⏎ .click();</code> → <code>await conflictHeading.focus();</code> |
| `tests/authenticated-e2e/runner-resilience.spec.ts:1149` | <code>second.getByRole(&quot;heading&quot;, { name: &quot;Choose the workout value to keep&quot; }),</code> → <code>second.getByRole(&quot;heading&quot;, { name: &quot;Pick which value to keep&quot; }),</code> |
| `tests/authenticated-e2e/runner-resilience.spec.ts:1155` | <code>(absent)</code> → <code>await second.getByText(&quot;More options&quot;, { exact: true }).click();</code> |
| `tests/authenticated-e2e/runner-resilience.spec.ts:1157` | <code>.getByLabel(/Private note for Dumbbell bench press/iu)</code> → <code>.getByLabel(&quot;Note&quot;, { exact: true })</code> |
| `tests/authenticated-e2e/runner-resilience.spec.ts:1160` | <code>await expect(second.getByRole(&quot;textbox&quot;, { name: /Private note/iu })).not.toHaveValue(</code> → <code>await expect(second.getByRole(&quot;textbox&quot;, { name: &quot;Note&quot;, exact: true })).not.toHaveValue(</code> |
| `tests/authenticated-e2e/runner-resilience.spec.ts:1183` | <code>await first.getByRole(&quot;button&quot;, { name: &quot;Complete workout&quot; }).click();</code> → <code>await first.getByRole(&quot;button&quot;, { name: &quot;Finish workout&quot; }).click();</code> |
| `tests/e2e/animal-surface-pilot.spec.ts:105` | <code>page.getByRole(&quot;heading&quot;, { level: 1, name: &quot;Your workout. Your way.&quot; }),</code> → <code>page.getByRole(&quot;heading&quot;, { level: 1, name: &quot;A little space for your next set.&quot; }),</code> |
| `tests/e2e/animal-surface-pilot.spec.ts:109` | <code>await expectNoHorizontalOverlap(page.locator(&quot;.landing-copy&quot;), placement);</code> → <code>await expectNoHorizontalOverlap(page.locator(&quot;.quiet-welcome-copy&quot;), placement);</code> |
| `tests/e2e/animal-surface-pilot.spec.ts:111` | <code>await expectNoIntersection(page.locator(&quot;.landing-copy&quot;), placement);</code> → <code>await expectNoIntersection(page.locator(&quot;.quiet-welcome-copy&quot;), placement);</code> |
| `tests/e2e/animal-surface-pilot.spec.ts:125` | <code>page.getByRole(&quot;heading&quot;, { level: 1, name: &quot;Your workout. Your way.&quot; }),</code> → <code>page.getByRole(&quot;heading&quot;, { level: 1, name: &quot;A little space for your next set.&quot; }),</code> |
| `tests/e2e/animal-surface-pilot.spec.ts:148` | <code>copy: &quot;.landing-copy&quot;, ⏎ heading: &quot;Your workout. Your way.&quot;,</code> → <code>copy: &quot;.quiet-welcome-copy&quot;, ⏎ heading: &quot;A little space for your next set.&quot;,</code> |
| `tests/e2e/animal-surface-pilot.spec.ts:188` | <code>await expectNoIntersection(placement, page.locator(&quot;.landing-actions&quot;));</code> → <code>await expectNoIntersection(placement, page.locator(&quot;.quiet-welcome-copy&quot;));</code> |
| `tests/e2e/animal-surface-pilot.spec.ts:191` | <code>page.getByText(&quot;Sample data · not your history&quot;, { exact: true }),</code> → <code>page.getByText(&quot;Example data&quot;, { exact: true }),</code> |
| `tests/e2e/animal-surface-pilot.spec.ts:323` | <code>page.locator(&quot;.landing-actions&quot;),</code> → <code>page.locator(&quot;.quiet-welcome-copy&quot;),</code> |
| `tests/e2e/animal-surface-pilot.spec.ts:349` | <code>page.locator(&quot;.landing-actions&quot;),</code> → <code>page.locator(&quot;.quiet-welcome-copy&quot;),</code> |
| `tests/e2e/animal-surface-rollout.spec.ts:73` | <code>if (width &lt; 1024) { ⏎ await expect(placement).toBeHidden();</code> → <code>if (width &lt;= 700) { ⏎ await expect(placement).toBeVisible();</code> |
| `tests/e2e/animal-surface-rollout.spec.ts:87` | <code>&quot;.guest-stamp&quot;,</code> → <code>&quot;.public-library-hero p&quot;,</code> |
| `tests/e2e/public-release.spec.ts:42` | <code>name: &quot;Your workout. Your way.&quot;,</code> → <code>name: &quot;A little space for your next set.&quot;,</code> |
| `tests/e2e/public-release.spec.ts:45` | <code>await expect(page.locator(&#x27;[data-companion-placement=&quot;landing&quot;]&#x27;)).toBeVisible();</code> → <code>await expect(page.locator(&#x27;.quiet-studio&#x27;)).toBeVisible();</code> |
| `tests/e2e/public-release.spec.ts:47` | <code>page.locator(&#x27;[data-companion-placement=&quot;landing&quot;] img&#x27;),</code> → <code>page.locator(&#x27;.quiet-studio img&#x27;),</code> |
| `tests/e2e/public-release.spec.ts:49` | <code>await expect(page.getByText(&quot;Open to everyone&quot;)).toBeVisible(); ⏎ await expect(page.getByText(&quot;Sign in to make it yours&quot;)).toBeVisible();</code> → <code>await expect(page.getByText(&quot;No account needed. Practice stays temporary.&quot;)).toBeVisible(); ⏎ await expect(page.getByRole(&quot;link&quot;, { name: &quot;Create my routine&quot; })).toBeVisible();</code> |
| `tests/e2e/public-release.spec.ts:62` | <code>await expect(page.getByText(&quot;Starter preview · not saved&quot;)).toBeVisible();</code> → <code>await expect(page.getByRole(&quot;link&quot;, { name: &quot;Sign in to save your own version&quot; })).toBeVisible();</code> |
| `tests/e2e/public-release.spec.ts:65` | <code>page.getByRole(&quot;button&quot;, { name: `${index + 1} ${name}` }),</code> → <code>page.getByRole(&quot;link&quot;, { name: new RegExp(`^Day ${index + 1} ${name} `) }),</code> |
| `tests/e2e/public-release.spec.ts:69` | <code>await page.getByRole(&quot;link&quot;, { name: &quot;Open Push day&quot; }).click();</code> → <code>await page.getByRole(&quot;link&quot;, { name: /^Day 1 Push / }).click();</code> |
| `tests/e2e/public-release.spec.ts:75` | <code>await page.getByRole(&quot;link&quot;, { name: &quot;Push day&quot; }).click(); ⏎ await expect(page).toHaveURL(/\/program\/push\?equipment=dumbbells$/); ⏎ await page.getByRole(&quot;link&quot;, { name: &quot;Five-day starter example&quot; }).click(); ⏎ await expect(page).toHaveURL(/\/program\?equipment=dumbbells$/);</code> → <code>await page.getByRole(&quot;link&quot;, { name: &quot;Exercise library&quot; }).click(); ⏎ await expect(page).toHaveURL(/\/library\?equipment=dumbbells$/); ⏎ await page.getByRole(&quot;link&quot;, { name: &quot;Example&quot;, exact: true }).click(); ⏎ await expect(page).toHaveURL(/\/program$/);</code> |
| `tests/e2e/public-release.spec.ts:80` | <code>const barbellPreview = page.getByRole(&quot;button&quot;, {</code> → <code>const barbellPreview = page.getByRole(&quot;link&quot;, {</code> |
| `tests/e2e/public-release.spec.ts:84` | <code>await expect(barbellPreview).toHaveAttribute(&quot;aria-pressed&quot;, &quot;true&quot;); ⏎ await page.getByRole(&quot;button&quot;, { name: &quot;2 Pull&quot; }).click(); ⏎ const selectedDay = page.locator(&quot;#selected-day-sheet&quot;);</code> → <code>await expect(barbellPreview).toHaveAttribute(&quot;aria-current&quot;, &quot;true&quot;); ⏎ await page.getByRole(&quot;link&quot;, { name: /^Day 2 Pull / }).focus(); ⏎ const selectedDay = page.locator(&quot;.program-day-grid &gt; li&quot;).filter({ has: page.getByRole(&quot;heading&quot;, { name: &quot;Pull&quot;, exact: true }) });</code> |
| `tests/e2e/public-release.spec.ts:88` | <code>selectedDay.getByRole(&quot;heading&quot;, { level: 2, name: &quot;Pull day&quot; }),</code> → <code>selectedDay.getByRole(&quot;heading&quot;, { level: 2, name: &quot;Pull&quot; }),</code> |
| `tests/e2e/public-release.spec.ts:90` | <code>await expect(selectedDay.getByText(&quot;Barbell bent-over row&quot;)).toBeVisible();</code> → <code>await expect(selectedDay.getByText(&quot;Barbell bent-over row&quot;, { exact: true })).toBeVisible();</code> |
| `tests/e2e/public-release.spec.ts:92` | <code>await selectedDay.getByRole(&quot;link&quot;, { name: &quot;Open Pull day&quot; }).click();</code> → <code>await selectedDay.getByRole(&quot;link&quot;, { name: /^Day 2 Pull / }).click();</code> |
| `tests/e2e/public-release.spec.ts:100` | <code>await expect(page.getByText(&quot;Starter preview · not saved&quot;)).toBeVisible();</code> → <code>await expect(page.getByText(&quot;Edit cardio targets once you save a routine.&quot;)).toBeVisible();</code> |
| `tests/e2e/public-release.spec.ts:113` | <code>await expect(page).toHaveURL(/\/library\?equipment=barbell&amp;q=bent\+over\+row$/);</code> → <code>await expect(page).toHaveURL(/\/library\?equipment=barbell$/);</code> |
| `tests/e2e/public-release.spec.ts:134` | <code>page.getByRole(&quot;heading&quot;, { level: 1, name: &quot;Lower workout&quot; }),</code> → <code>page.getByRole(&quot;heading&quot;, { level: 1, name: &quot;Example workout&quot; }),</code> |
| `tests/e2e/public-release.spec.ts:143` | <code>await expect(page.getByText(&quot;Sample data · not your history&quot;, { exact: true })).toHaveCount(1);</code> → <code>await expect(page.getByText(&quot;Example data&quot;, { exact: true })).toHaveCount(1);</code> |
| `tests/e2e/public-release.spec.ts:150` | <code>/^(Sign-in connection pending&#124;Sign in)$/,</code> → <code>/^(Sign-in is unavailable&#124;Sign in)$/,</code> |
| `tests/e2e/public-release.spec.ts:152` | <code>await expect(page.getByRole(&quot;link&quot;, { name: &quot;Browse the free program&quot; })).toBeVisible();</code> → <code>await expect(page.getByRole(&quot;link&quot;, { name: &quot;Explore the example routine&quot; })).toBeVisible();</code> |
| `tests/e2e/public-release.spec.ts:164` | <code>name: &quot;My workouts&quot;,</code> → <code>name: &quot;Sign in&quot;,</code> |
| `tests/e2e/public-release.spec.ts:166` | <code>await expect(landingAccountAction).toHaveAttribute(&quot;href&quot;, &quot;/app&quot;);</code> → <code>await expect(landingAccountAction).toHaveAttribute(&quot;href&quot;, &quot;/sign-in&quot;);</code> |
| `tests/e2e/public-release.spec.ts:168` | <code>await expect(page).toHaveURL(/\/sign-in\?returnTo=%2Fapp$/u); ⏎ await expect(page.locator(&quot;#auth-heading&quot;)).toHaveText(/^(Sign-in connection pending&#124;Sign in)$/);</code> → <code>await expect(page).toHaveURL(/\/sign-in$/u); ⏎ await expect(page.locator(&quot;#auth-heading&quot;)).toHaveText(/^(Sign-in is unavailable&#124;Sign in)$/);</code> |
| `tests/e2e/public-release.spec.ts:173` | <code>await expect(page.getByText(&quot;Starter preview · not saved&quot;)).toBeVisible(); ⏎ const programAccountActions = page.getByRole(&quot;link&quot;, { name: &quot;My workouts&quot; }); ⏎ await expect(programAccountActions.first()).toHaveAttribute(&quot;href&quot;, &quot;/app&quot;);</code> → <code>await expect(page.getByRole(&quot;link&quot;, { name: &quot;Sign in to save your own version&quot; })).toBeVisible(); ⏎ const programAccountActions = page.getByRole(&quot;link&quot;, { name: &quot;Sign in&quot; }); ⏎ await expect(programAccountActions.first()).toHaveAttribute(&quot;href&quot;, &quot;/sign-in&quot;);</code> |
| `tests/e2e/public-release.spec.ts:178` | <code>await expect(page.getByText(&quot;Starter preview · not saved&quot;)).toBeVisible(); ⏎ await expect(page.getByText(&quot;Five-day starter example&quot;)).toBeVisible(); ⏎ const dayAccountAction = page.getByRole(&quot;link&quot;, { name: &quot;My workouts&quot; }); ⏎ await expect(dayAccountAction).toHaveAttribute(&quot;href&quot;, &quot;/app&quot;);</code> → <code>await expect(page.getByText(/\d+ movements with a walker or runner finish\./)).toBeVisible(); ⏎ await expect(page.getByText(&quot;Example routine&quot;)).toBeVisible(); ⏎ const dayAccountAction = page.getByRole(&quot;link&quot;, { name: &quot;Sign in&quot; }); ⏎ await expect(dayAccountAction).toHaveAttribute(&quot;href&quot;, &quot;/sign-in&quot;);</code> |
| `tests/e2e/public-release.spec.ts:183` | <code>await expect(page).toHaveURL(/\/sign-in\?returnTo=%2Fapp$/u); ⏎ await expect(page.locator(&quot;#auth-heading&quot;)).toHaveText(/^(Sign-in connection pending&#124;Sign in)$/);</code> → <code>await expect(page).toHaveURL(/\/sign-in$/u); ⏎ await expect(page.locator(&quot;#auth-heading&quot;)).toHaveText(/^(Sign-in is unavailable&#124;Sign in)$/);</code> |
| `tests/e2e/public-release.spec.ts:245` | <code>.getByRole(&quot;link&quot;, { name: &quot;Explore the five-day example&quot; })</code> → <code>.getByRole(&quot;link&quot;, { name: &quot;Try one set&quot; })</code> |
| `tests/e2e/public-release.spec.ts:264` | <code>name: &quot;Your workout. Your way.&quot;,</code> → <code>name: &quot;A little space for your next set.&quot;,</code> |
| `tests/e2e/public-release.spec.ts:276` | <code>expect(presentation.backgroundColor).toBe(&quot;rgb(11, 37, 43)&quot;);</code> → <code>expect(presentation.backgroundColor).toBe(&quot;rgb(20, 42, 35)&quot;);</code> |
| `tests/e2e/public-release.spec.ts:281` | <code>const controls = page.locator(&quot;.equipment-control button&quot;);</code> → <code>const controls = page.locator(&quot;.profile-links a&quot;);</code> |
| `tests/e2e/pwa-offline.spec.ts:37` | <code>page.getByRole(&quot;heading&quot;, { level: 1, name: &quot;Your workout. Your way.&quot; }),</code> → <code>page.getByRole(&quot;heading&quot;, { level: 1, name: &quot;A little space for your next set.&quot; }),</code> |
| `tests/e2e/pwa-offline.spec.ts:124` | <code>await expect(page.locator(&quot;.offline-indicator&quot;)).toContainText(&quot;Offline&quot;);</code> → <code>await expect(page.locator(&quot;.offline-indicator&quot;)).toContainText(&quot;offline&quot;);</code> |
| `tests/e2e/pwa-offline.spec.ts:128` | <code>page.getByRole(&quot;heading&quot;, { level: 1, name: &quot;Your workout. Your way.&quot; }),</code> → <code>page.getByRole(&quot;heading&quot;, { level: 1, name: &quot;A little space for your next set.&quot; }),</code> |
| `tests/e2e/pwa-offline.spec.ts:133` | <code>.locator(&#x27;[data-companion-placement=&quot;landing&quot;] img&#x27;)</code> → <code>.locator(&#x27;.quiet-studio img&#x27;)</code> |
| `tests/e2e/pwa-offline.spec.ts:154` | <code>await expect(page.locator(&quot;.offline-indicator&quot;)).toContainText(&quot;Offline&quot;);</code> → <code>await expect(page.locator(&quot;.offline-indicator&quot;)).toContainText(&quot;offline&quot;);</code> |
| `tests/unit/accessible-labels.test.tsx:65` | <code>expect(explorerMarkup).toContain(&quot;Five-day starter example&quot;);</code> → <code>expect(explorerMarkup).toContain(&quot;Five-day example routine&quot;);</code> |
| `tests/unit/accessible-labels.test.tsx:73` | <code>expect(explorerMarkup).toContain(`&lt;strong&gt;${number}&lt;/strong&gt;&lt;span&gt;${day}&lt;/span&gt;`);</code> → <code>expect(explorerMarkup).toContain(`&lt;small&gt;Day ${number}&lt;/small&gt;&lt;h2&gt;${day}&lt;/h2&gt;`);</code> |
| `tests/unit/accessible-labels.test.tsx:112` | <code>&#x27;id=&quot;selected-day-sheet&quot; tabindex=&quot;-1&quot;&#x27;,</code> → <code>&#x27;id=&quot;main-content&quot; tabindex=&quot;-1&quot;&#x27;,</code> |
| `tests/unit/auth-navigation-boundary.test.ts:21` | <code>expect(settings).toContain(&#x27;window.location.replace(&quot;/sign-in&quot;)&#x27;);</code> → <code>expect(settings).not.toContain(&#x27;window.location.replace(&quot;/sign-in&quot;)&#x27;);</code> |
| `tests/unit/authenticated-program-customization-fixture-boundary.test.ts:84` | <code>/\.program-editor-section-actions button, \.program-editor-add-section button \{[^}]*min-height: 2\.75rem;/u,</code> → <code>/\.program-editor-add-section button \{[^}]*min-height: 2\.75rem;/u,</code> |
| `tests/unit/catalog-text-only-presentation.test.tsx:62` | <code>expect(markup).toContain(&quot;No demonstration is available&quot;);</code> → <code>expect(markup).toContain(&quot;No video yet. Follow the steps below.&quot;);</code> |
| `tests/unit/curated-video-publication.test.tsx:105` | <code>expect(unavailable).toContain(&quot;No demonstration is available&quot;);</code> → <code>expect(unavailable).toContain(&quot;No video yet. Follow the steps below.&quot;);</code> |
| `tests/unit/curated-video-publication.test.tsx:216` | <code>expect(publicMarkup).toContain(&quot;No demonstration is available&quot;);</code> → <code>expect(publicMarkup).toContain(&quot;No video yet. Follow the steps below.&quot;);</code> |
| `tests/unit/firebase-client-identity-status.test.tsx:15` | <code>expect(markup).toContain(&quot;Checking the browser Firebase sign-in&quot;);</code> → <code>expect(markup).toContain(&quot;Checking…&quot;);</code> |
| `tests/unit/firebase-client-identity-status.test.tsx:28` | <code>expect(markup).toContain(&quot;ready for same-account reauthentication&quot;);</code> → <code>expect(markup).toBe(&quot;&quot;);</code> |
| `tests/unit/firebase-client-identity-status.test.tsx:33` | <code>[&quot;missing&quot;, &quot;could not be found&quot;], ⏎ [&quot;mismatch&quot;, &quot;does not match this secure server session&quot;],</code> → <code>[&quot;missing&quot;, &quot;Please sign in again to delete your account.&quot;], ⏎ [&quot;mismatch&quot;, &quot;Please sign in again to delete your account.&quot;],</code> |
| `tests/unit/firebase-client-identity-status.test.tsx:57` | <code>expect(markup).toContain(&quot;did not finish loading safely&quot;); ⏎ expect(markup).toContain(&quot;Retry Firebase check&quot;);</code> → <code>expect(markup).toContain(&quot;Something went wrong. Try again.&quot;); ⏎ expect(markup).toContain(&quot;Try again&quot;);</code> |
| `tests/unit/member-home-route-states.test.tsx:11` | <code>expect(markup).toContain(&quot;Loading your home…&quot;); ⏎ expect(markup).toContain(&quot;routine, saved progress, and any resumable workout&quot;);</code> → <code>expect(markup).toContain(&quot;Loading…&quot;); ⏎ expect(markup).not.toContain(&quot;routine, saved progress, and any resumable workout&quot;);</code> |
| `tests/unit/member-home-route-states.test.tsx:22` | <code>expect(markup).toContain(&quot;Your home did not load.&quot;); ⏎ expect(markup).toContain(&quot;No routine or workout changes were made.&quot;);</code> → <code>expect(markup).toContain(&quot;This page didn&amp;#x27;t load&quot;); ⏎ expect(markup).toContain(&quot;Nothing was changed.&quot;);</code> |
| `tests/unit/member-program-home.test.tsx:116` | <code>expect(markup).toContain(&quot;1 movement · no cardio&quot;); ⏎ expect(markup).toContain(&quot;1 movement · 1 cardio option&quot;);</code> → <code>expect(markup).toContain(&quot;&lt;small&gt;1 movement&lt;/small&gt;&quot;);</code> |
| `tests/unit/member-program-home.test.tsx:118` | <code>expect(markup).toContain(&quot;Open Mobility to start&quot;);</code> → <code>expect(markup).not.toContain(&quot;Open Mobility to start&quot;);</code> |
| `tests/unit/member-program-home.test.tsx:175` | <code>expect(markup).toContain(&quot;Review Mobility&quot;);</code> → <code>expect(markup).toContain(&#x27;href=&quot;/app/program/70000000-0000-4000-8000-000000000001&quot;&gt;&lt;span&gt;01&lt;/span&gt;&lt;strong&gt;Mobility&lt;/strong&gt;&#x27;);</code> |
| `tests/unit/member-program-home.test.tsx:200` | <code>expect(markup).not.toContain(&#x27;href=&quot;/app/program/70000000-0000-4000-8000-000000000001&quot;&#x27;);</code> → <code>expect(markup).toContain(&#x27;href=&quot;/app/program/70000000-0000-4000-8000-000000000001&quot;&#x27;);</code> |
| `tests/unit/program-collection-component.test.tsx:43` | <code>expect(markup.match(/Active program/g)).toHaveLength(1);</code> → <code>expect(markup.match(/&gt;Active&lt;/g)).toHaveLength(1);</code> |
| `tests/unit/program-collection-component.test.tsx:46` | <code>expect(markup).toContain(&quot;Create from example&quot;);</code> → <code>expect(markup).toContain(&quot;Create and use this routine&quot;);</code> |
| `tests/unit/program-collection-component.test.tsx:51` | <code>expect(markup).toContain(&quot;Clone&quot;);</code> → <code>expect(markup).toContain(&quot;Duplicate&quot;);</code> |
| `tests/unit/program-collection-component.test.tsx:61` | <code>expect(markup).toContain(&quot;Active overview&quot;);</code> → <code>expect(markup).toContain(&quot;Back to Today&quot;);</code> |
| `tests/unit/program-collection-model.test.ts:167` | <code>expect(() =&gt; validatedProgramName(&quot;   &quot;)).toThrow(&quot;Enter a program name&quot;);</code> → <code>expect(() =&gt; validatedProgramName(&quot;   &quot;)).toThrow(&quot;Enter a routine name&quot;);</code> |
| `tests/unit/program-editor-model.test.ts:402` | <code>pace: &quot;seconds / km&quot;,</code> → <code>pace: &quot;min / km&quot;,</code> |
| `tests/unit/program-editor-model.test.ts:407` | <code>pace: &quot;seconds / mile&quot;,</code> → <code>pace: &quot;min / mile&quot;,</code> |
| `tests/unit/public-account-entry.test.tsx:52` | <code>href: &quot;/app&quot;, ⏎ label: expect.stringContaining(&quot;My workouts&quot;),</code> → <code>href: &quot;/sign-in&quot;, ⏎ label: expect.stringContaining(&quot;Sign in&quot;),</code> |
| `tests/unit/public-account-entry.test.tsx:70` | <code>expect(observedLinks.filter(({ label }) =&gt; label.includes(&quot;My workouts&quot;)))</code> → <code>expect(observedLinks.filter(({ label }) =&gt; label.includes(&quot;Sign in&quot;)))</code> |
| `tests/unit/public-account-entry.test.tsx:72` | <code>{ href: &quot;/app&quot;, label: expect.stringContaining(&quot;My workouts&quot;), prefetch: false }, ⏎ { href: &quot;/app&quot;, label: expect.stringContaining(&quot;My workouts&quot;), prefetch: false },</code> → <code>{ href: &quot;/sign-in&quot;, label: expect.stringContaining(&quot;Sign in&quot;), prefetch: false }, ⏎ { href: &quot;/sign-in&quot;, label: expect.stringContaining(&quot;Sign in to save your own version&quot;), prefetch: undefined },</code> |
| `tests/unit/public-account-entry.test.tsx:81` | <code>expect(explorer).toContain(&quot;Five-day starter example&quot;); ⏎ expect(explorer).toContain(&quot;Starter preview · not saved&quot;); ⏎ expect(day).toContain(&quot;Five-day starter example&quot;); ⏎ expect(day).toContain(&quot;Starter preview · not saved&quot;); ⏎ expect(day).toContain(&#x27;href=&quot;/app&quot;&#x27;); ⏎ expect(day).toContain(&quot;My workouts&quot;);</code> → <code>expect(explorer).toContain(&quot;Five-day example routine&quot;); ⏎ expect(explorer).not.toContain(&quot;Starter preview · not saved&quot;); ⏎ expect(day).toContain(&quot;Example routine&quot;); ⏎ expect(day).not.toContain(&quot;Starter preview · not saved&quot;); ⏎ expect(day).toContain(&#x27;href=&quot;/sample-workout&quot;&#x27;); ⏎ expect(day).toContain(&#x27;PublicShell current=&quot;program&quot;&#x27;);</code> |
| `tests/unit/public-account-entry.test.tsx:92` | <code>expect(contents).not.toContain(&#x27;href=&quot;/sign-in&quot;&#x27;); ⏎ expect(contents).toContain(&#x27;href=&quot;/app&quot;&#x27;); ⏎ expect(contents).toMatch(/href=&quot;\/app&quot;\s+prefetch=\{false\}/u);</code> → <code>expect(contents).not.toContain(path.includes(&quot;sample-workout&quot;) ? &#x27;href=&quot;/app&quot;&#x27; : &#x27;href=&quot;/sign-in&quot;&#x27;); ⏎ expect(contents).toContain(path.includes(&quot;sample-workout&quot;) ? &#x27;href=&quot;/sign-in&quot;&#x27; : &#x27;href=&quot;/app&quot;&#x27;); ⏎ expect(contents).toMatch(path.includes(&quot;sample-workout&quot;) ? /href=&quot;\/sign-in&quot;\s+prefetch=\{false\}/u : /href=&quot;\/app&quot;\s+prefetch=\{false\}/u);</code> |
| `tests/unit/public-progress-page.test.tsx:18` | <code>expect(markup).toContain(&quot;Sample data · not your history&quot;); ⏎ expect(markup.match(/Sample data · not your history/g)).toHaveLength(1);</code> → <code>expect(markup).toContain(&quot;Example data&quot;); ⏎ expect(markup.match(/Example data/g)).toHaveLength(1);</code> |
| `tests/unit/settings-form-auth-readiness.test.tsx:36` | <code>expect(markup).toContain(&quot;Checking the browser Firebase sign-in&quot;); ⏎ expect(markup.match(/Checking the browser Firebase sign-in/g)).toHaveLength(1); ⏎ expect(markup).toMatch(/&lt;button[^&gt;]*disabled=&quot;&quot;[^&gt;]*&gt;Review permanent deletion&lt;\/button&gt;/);</code> → <code>expect(markup).toContain(&quot;Checking…&quot;); ⏎ expect(markup.match(/Checking…/g)).toHaveLength(1); ⏎ expect(markup).toMatch(/&lt;button[^&gt;]*disabled=&quot;&quot;[^&gt;]*&gt;Delete my account&lt;\/button&gt;/);</code> |
| `tests/unit/strength-catalog-expansion.test.tsx:280` | <code>expect(markup).toContain(&quot;No demonstration is available&quot;);</code> → <code>expect(markup).toContain(&quot;No video yet. Follow the steps below.&quot;);</code> |
| `tests/unit/training-insights-components.test.tsx:137` | <code>expect(markup).toContain(&quot;Read-only snapshot.&quot;);</code> → <code>expect(markup).not.toContain(&quot;Read-only snapshot.&quot;);</code> |
| `tests/unit/training-insights-components.test.tsx:155` | <code>expect(markup).toContain(&quot;Tied best · 21 exact source sets&quot;);</code> → <code>expect(markup).toContain(&quot;Tied best (21 times)&quot;);</code> |
| `tests/unit/workout-runner-component-harness.test.tsx:200` | <code>expect(oneMarkup).toContain(&quot;Configured cardio option&quot;); ⏎ expect(oneMarkup).toContain(&quot;Choose the configured cardio option&quot;);</code> → <code>expect(oneMarkup).toContain(&quot;Cardio finish&quot;); ⏎ expect(oneMarkup).toContain(&quot;Choose Walker or Runner to log it.&quot;);</code> |
| `tests/unit/workout-runner-component-harness.test.tsx:205` | <code>expect(twoMarkup).toContain(&quot;Configured cardio options (2)&quot;); ⏎ expect(twoMarkup).toContain(&quot;Choose one of the 2 configured cardio options&quot;);</code> → <code>expect(twoMarkup).toContain(&quot;Cardio finish&quot;); ⏎ expect(twoMarkup).toContain(&quot;Choose Walker or Runner to log it.&quot;);</code> |
| `tests/unit/workout-runner-component-harness.test.tsx:424` | <code>expect(markup).toContain(&quot;Choose the workout value to keep&quot;);</code> → <code>expect(markup).toContain(&quot;Pick which value to keep&quot;);</code> |
| `tests/unit/workout-runner-component-harness.test.tsx:427` | <code>&#x27;aria-label=&quot;Keep 20 kg · 10 reps for Set 1 · Floor press&quot;&#x27;,</code> → <code>&#x27;aria-label=&quot;Keep 20 kg · 10 reps&quot;&#x27;,</code> |
| `tests/unit/workout-runner-component-harness.test.tsx:430` | <code>&#x27;aria-label=&quot;Keep 25 kg · 8 reps for Set 1 · Floor press&quot;&#x27;,</code> → <code>&#x27;aria-label=&quot;Keep 25 kg · 8 reps&quot;&#x27;,</code> |
| `tests/unit/workout-runner-component-harness.test.tsx:432` | <code>expect(markup).toContain(&quot;Leave both values unresolved&quot;);</code> → <code>expect(markup).not.toContain(&quot;Leave both values unresolved&quot;);</code> |
| `tests/unit/workout-runner-component-harness.test.tsx:483` | <code>expect(markup).toContain(&quot;Reauthenticate and return&quot;);</code> → <code>expect(markup).toContain(&quot;Sign in again&quot;);</code> |
| `tests/unit/workout-runner-component-harness.test.tsx:506` | <code>expect(revokedMarkup).toContain(&quot;Sign-in revoked&quot;);</code> → <code>expect(revokedMarkup).toContain(&quot;Sign in again&quot;);</code> |
| `tests/unit/workout-runner-presenters.test.ts:92` | <code>label: &quot;Offline queued&quot;,</code> → <code>label: &quot;Saved on this device&quot;,</code> |
| `tests/unit/workout-runner-presenters.test.ts:96` | <code>label: &quot;Sign-in expired&quot;,</code> → <code>label: &quot;Sign in again&quot;,</code> |
| `tests/unit/workout-runner-presenters.test.ts:100` | <code>label: &quot;Sign-in revoked&quot;,</code> → <code>label: &quot;Sign in again&quot;,</code> |
| `tests/unit/workout-runner.test.ts:1040` | <code>expect(isNavigationBlocked(queued)).toBe(true);</code> → <code>expect(isNavigationBlocked(queued)).toBe(false);</code> |
| `tests/unit/workout-runner.test.ts:1457` | <code>expect(isNavigationBlocked(completing)).toBe(true);</code> → <code>expect(isNavigationBlocked(completing)).toBe(false);</code> |
| `tests/unit/workout-runner.test.ts:1522` | <code>).toThrow(/local draft&#124;confirmed/);</code> → <code>).toThrow(/Save your edited set or note first/);</code> |
| `tests/unit/workout-runner.test.ts:1538` | <code>).toThrow(/confirmed/);</code> → <code>).toThrow(/Wait for your last changes to save/);</code> |
| `tests/unit/workout-runner.test.ts:1627` | <code>/Explicitly complete/,</code> → <code>/Finish or skip/,</code> |
| `tests/unit/workout-runner.test.ts:1639` | <code>/confirmed/,</code> → <code>/Wait for your last changes to save/,</code> |
| `tests/unit/workout-runner.test.ts:1721` | <code>/required cardio/,</code> → <code>/Log your cardio finish first/,</code> |
| `tests/unit/workout-runner.test.ts:1731` | <code>/required cardio/,</code> → <code>/Log your cardio finish first/,</code> |
| `tests/unit/workout-runner.test.ts:1736` | <code>/required cardio/,</code> → <code>/Log your cardio finish first/,</code> |
| `tests/unit/workout-runner.test.ts:1751` | <code>expect(isNavigationBlocked(state)).toBe(true);</code> → <code>expect(isNavigationBlocked(state)).toBe(false);</code> |
| `tests/unit/workout-runner.test.ts:1847` | <code>expect(next.restTimer).toBeUndefined();</code> → <code>expect(next.restTimer).toBe(state.restTimer);</code> |

## 4. Noticed, not changed

On committed closeout `b977eab`, W3 reported `1 failed` and `11 passed (20.6s)`. The phone page sweep failed at `page.goto("/try")` with `net::ERR_ABORTED`; the server log also reported `Router action dispatched before initialization` and a Fast Refresh full reload. The geometry assertions did not fail. The same unchanged command had passed all 12 cases in preflight. This is a development-server reliability limit, not evidence of a passed final W3 attempt. No acceptance assertion, framework setting, dependency, retry policy, or timeout was changed. The final serial run on the report commit is disclosed separately in the PR and owner handoff, whether it passes or fails.

The stopped items above preserve the owner's test boundary. Fixing the retired composition checks, restoring deleted explanatory content, or changing the reset-only contract would require a new scope decision. No geometry limits, successful-retry assertions, visibility checks, or storage-unit assertions were deleted to obtain green results.

The routine collection still describes new routines as "private, independent revisions" in its explanatory paragraph. This wording was visible in the refreshed screenshot; it is outside the current bounded W5 follow-up and is recorded for copy review.

The local app can receive text input before a newly navigated form finishes hydrating. A focused diagnostic created a routine under its default name; the current collection journey selects equipment before entering its routine name, preserving the intended inputs and name-specific checks. That focused run was interrupted after the mismatch was identified; neither full main W5 suite nor full branch W5 suite was stopped early. The broader hydration behavior was not changed outside this plan.

The initial main release build encountered stale generated `.next/dev/types` artifacts. After moving that cache aside, the unchanged main release command completed all 132 cases. Browser and build results here are local evidence; hosted provider behavior and production deployment remain untested under the explicit boundary.

## 5. Pull request and closeout

[PR #8 — Production-grade audit fixes](https://github.com/vdoshi96/My-Workout-Pal/pull/8) targets `main` from `vishal/production-grade-audit`. The branch is pushed and the PR is marked ready for review at the owner's explicit request, with W5 failures disclosed. No merge or deployment command runs. Main remains at `c619ac6`. The canonical checkout is the sole worktree and remains active for review and the unresolved stopped items; no extra branch or worktree was created.
